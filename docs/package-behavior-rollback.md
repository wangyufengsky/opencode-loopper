# 0.3.80：撤回未达标的工作包语义实验

用户于 2026-09-08 要求回退未成功的升级。本次撤回 `0eb84dc`、`98d3ea2`、`d9f05cf` 的实验功能，工作包生产 Java、MCP Schema、Prompt 和角色编译路径恢复到 `958506e`（0.3.75）。交付版本使用递增的 0.3.80，不覆盖旧 JAR，也不重写 Git 历史。

## 回退与保留

- 移除行为义务内核、SAT4J 依赖、`behaviorBranches` 扩展、独立来源复核、相关配置及生产调度路径，不再保留可重新启用的实验开关。
- 保留 0.3.75 已默认启用的 V2、冻结来源、all/any/unless 关系、确定性范围与删除保护、原有可选一次整理和同会话修复。V1/旧 Role Pack 兼容路径不变。
- 保留 0.3.79 的 Designer 页面测试自动卸载清理，它只修复测试环境遗留轮询，不改变产品行为。
- 评测文档、语料和结果仅作为历史证据保留；相关编译器和离线驱动恢复到回退基线，不使用已经移除的实验接口。三次原提交和旧 JAR 不删除。

回退依据是固定 Luna 对照中复杂案例四投内正确通过率从 15/24 降到 9/24，并存在编译接受后的语义表达不足。详见 [历史验收记录](package-behavior-qualification.md)。回退不表示 0.3.75 已解决自然语言语义完整性，也不产生新的模型通过率结论。

## 数据库边界

V74 迁移保持逐字节不变，避免已经执行 V74 的数据库因缺失迁移或校验和变化无法正常校验。新增 V75 不删除业务或实验数据：没有实验策略、准备、义务运行、独立复核会话的数据库可继续迁移；若存在任一实验记录，则以 `PACKAGE_BEHAVIOR_HISTORY_REQUIRES_0_3_79` 拒绝启动并回滚本次迁移。不能把停止、失败或已接受的实验合同静默改成普通 V2。

含实验记录的库应继续使用 0.3.79 与其原配置处理和查看。不要删除实验表、改写 Flyway 历史或直接把已冻结运行降级；如确实要迁移这类数据库，必须另行设计并验证明确的数据迁移方案。新版本不自动终止远端或删除证据。

回退前只读检查：本地 `data/loopper.db` 最高迁移为 V73，无 V74 实验表；8080 无监听。本次不修改该数据库，不启动、部署或重启服务。测试使用临时 SQLite，覆盖 V73/空 V74 升级与非空实验记录拒绝。

## 验证

聚焦覆盖 V2 正常编译、被撤回的字段拒绝、历史合同、范围守卫、MCP 同会话修复、迁移及前端清理；随后执行完整 `./scripts/verify.sh`。成品检查 V2 默认 true、不包含 SAT4J 或实验 Java 类，保留 V74/V75，前端资源存在。结果和 SHA-256 记录在 AGENTS 维护记录及本地交付回执中。
