# 0.4.76 角色配置、发布与管理

## 范围与基线

按照用户明确选择，将 `codex/source-template-tasks` 的 0.4.74 快进合入 `main`（基线 `78aa086b`），再集成角色配置重构。保留来源模板、单元测试与详细设计功能，以及已执行的 V127–V138；角色表仅追加 V139。合并前本任务改动的安全 stash 保留。

角色目录包含 26 个业务与辅助角色、52 个工作流职责位置，覆盖 39 个 SessionProfile 和提交说明、合并建议、统计等辅助调用。YAML、Markdown 与 Role Pack 片段经校验发布为数据库不可变修订；工作流绑定以 CAS 激活，角色 ID 可通过配置扩展。模型与运行策略引用保持原工作流的显式模型选择和独立预算语义。

Task、Designer、模板、文档、来源模板、知识会话及 PPT 运行在创建/冻结边界保存角色绑定，派生和恢复继承冻结版本；历史流程不补造 Prompt。权限保留服务端适配上限、当前项目策略及任务/阶段授权，角色仅能收窄；私有候选 MCP 和直接工具入口均检查冻结权限。本次未新增知识库访问授权。

设置新增 `/settings/roles`，支持角色用途、阶段、权限、MCP、静态 Prompt、来源、修订历史与差异，按版本导出 ZIP，以及导入校验、差异确认、幂等发布和冲突重试。Task 会话按需显示归属校验后的冻结角色摘要。首版不提供在线创建/编辑表单；新增阶段、候选协议和验收编译器仍需代码。

## 验证

- 角色核心、运行适配、迁移、来源模板等聚焦回归 194 项通过；受影响集成回归 285 项通过。
- 角色描述更新后再跑配置解析、发布、API 与工作流聚焦回归 21 项通过。
- 前端相关 5 个文件、27 项聚焦测试与类型检查通过；浏览器 E2E 9 项通过，覆盖深链接刷新、版本差异、失败保留、409 重新校验、空态和三种皮肤的 1440px/390px 布局。E2E 使用模拟 API；正式 JAR 的 HTTP 与浏览器证据另行记录。
- 门禁使用固定 Node 22.14.0；ZIP Blob API 测试另在 Node 24.19.0 通过，避免浏览器/Node 构造器差异造成误报。
- `./scripts/verify.sh`：0.4.76 BUILD SUCCESS，耗时 22:03；Java 2372 项，0 失败、0 错误、3 条件跳过；前端 101 文件 / 606 项全部通过；统计 guard 与项目检查通过。442 个候选源码/测试/文档文件在门禁期间的内容摘要保持一致。
- 最终 JAR 的隔离 HTTP 验收通过：26 角色 / 52 绑定；查询、预估与校验无写入；导出、真实 ZIP 发布、幂等重放、CAS 拒绝及失败无部分状态均符合预期。
- 两个同时竞争同一绑定的 HTTP 发布分别返回 200 / 409，只新增一份修订和回执；多绑定包中一个绑定过期时整批拒绝。未知字段、YAML 标签/别名、路径穿越、重复条目、压缩/单项/总展开字节与条目数超限共 9 类非法输入均返回 400，未留下写入。
- 真实 JAR 浏览器完成 ZIP 上传 → 校验与差异 → 确认发布，显示第 2 版配置和当前绑定；权限页明确显示只允许 read、保护路径及预估不完整原因；版本历史正确显示两版职责说明差异。应用重启后第 2 版内容摘要及绑定版本仍一致。隔离进程 35017、35107 均已停止；最初 Fake 测试使用生产启动器因缺少 CLI 字段退出，日志保留，随后用独立 Java 测试进程完成隔离验收；交付服务使用正式启动器。

Prompt 片段保留原 Java 文本的拼接空格和空行；`.gitattributes` 仅为该目录的 Markdown 关闭这两类格式告警，其他文件维持默认检查。最终暂存区 `git diff --cached --check` 通过，未改写已核验的 Prompt 字节或 JAR。

测试直接证明配置后的新 Router/来源模板请求使用新 Prompt，旧拥有者继续使用旧修订；收窄工具后的会话和 fork 权限一致；全部内置 Profile 的有序权限保持等价；V126/V138 升级保留旧业务记录且不补造角色快照。发布覆盖幂等键身份、CAS 冲突和事务回滚；查询/预估/校验不登记工具策略、不创建会话、不调用 Provider。

初次完整门禁暴露新增 V139 后的旧迁移计数断言、滚动规划 mock 签名及基于错误起点的超时测试夹具，已修正并保留失败日志。后续固定 Node 工具链发现 Blob 跨运行环境断言问题，改为类型和字节验证。0.4.75 完整候选曾通过全部门禁；实际浏览器检查后补充了角色职责说明，因此按公约递增为 0.4.76 并重跑完整门禁。

## 证据与限制

证据目录：`data/role-configuration-acceptance-20260924/`，包含各轮日志、候选文件摘要、截图、隔离 API/并发发布证据、产物校验及服务更新记录。0.4.70 和 0.4.75 旧候选 JAR 已保存在构建清理范围之外，当前服务使用的 JAR 未被 clean 覆盖。

本次证明配置、权限、发布、冻结和恢复协议的正确性；模型相关测试使用 Fake/隔离运行时，不代表真实 Provider 生成效果。未执行真实模型资格矩阵、Windows 全应用运行或外部 CupXml2Java 资格夹具；完整 Java 门禁对应三项条件跳过。未推送、创建远端标签、Release 或远端部署。


## JAR 与本地服务

- 构建 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/target/opencode-loopper-0.4.76.jar`，351631536 字节。
- SHA-256：`59a5d8eced299bf5f3ccebe2d3976f242557a9e5a640e829b5539164979fd780`。ZIP CRC 完整性、manifest 版本、首页、148 项 assets、角色清单与 V139 迁移均已核对。
- 运行 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.76.jar`，与构建 JAR 哈希一致。
- 旧 JVM 22074 和受管 OpenCode 22145 已正常退出，停止证明已保存。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper-data-backups/53156-before-0.4.76-20260924-143218`；236 个文件、137776982 字节逐项核对，备份 SQLite 完整性为 ok。
- 沿用原 launchd label、正式 macOS 启动器、JDK、端口、数据目录及配置。实际版本 0.4.76，JVM PID 35261，受管 OpenCode PID 35327，健康 UP、OpenCode AVAILABLE、内部 MCP CONNECTED。
- 地址：`http://127.0.0.1:53156/settings/roles`。数据库从 V138 正常追加 V139，新增 26 个角色及 52 个绑定。全部原有业务表计数和状态保持，配置哈希相同；旧拥有者/会话角色快照数为零，没有补造历史信息。

## 正式服务浏览器验收

从设置入口打开角色管理，确认中文职责、内置来源、当前阶段绑定、静态 Prompt 清单及正文；深链接刷新正常。390px 下页面内容宽度等于视口宽度，无横向溢出，随后恢复正常窗口。

模板任务仍显示七个模板，来源详细设计页面保留源码与输出目录、补充要求和预检入口；历史仍显示六条模板记录。已有代码审查任务与会话可以打开，展开角色摘要后明确显示“此会话创建时未记录角色配置快照”，保留历史执行合同。

正式服务上未导入测试角色，也未创建或启动真实模型任务；浏览器检查前后业务表计数、状态和配置哈希保持一致。角色管理页面保留为交付入口，服务继续运行。
