# 0.4.73 源码路径模板任务

状态：源码、自动验证、完整 JAR、本地服务和两项真实模型验收已完成。首次失败及其冻结预算保留；用户授权的一次修复后单测试跑通过。推送、不可变标签、Release Actions 和远端资产核验均已完成。

## 范围

新增“单元测试开发”和“详细设计编写”。输入项目内目录或单个源码文件，预检后创建待启动记录，正式启动时冻结当前源码。单测复用 Designer、滚动工作包、Task、真实验证器和双 Judge；只允许测试源码与夹具写入，业务代码、构建配置和已有测试受独立守卫保护。详细设计使用只读作者与独立复核角色，输出 Markdown 总览、模块详情、Mermaid 与源码覆盖清单，支持安全预览、单篇和整包下载。

持久化覆盖源码清单、模型身份、预算、恢复和制品；新增 V127–V138 迁移，保留历史候选身份与旧模板流程。统一任务历史合并源码发起与关联 Task，分页、筛选、归档范围一致。产品合同见 [源码模板合同](../source-template-contract.md)，实施账本见 [Goal 账本](source-template-goal-plan.md)。

## 已完成验证

- 源码与旧文档链路联合聚焦回归 72 项通过；归档、迁移、执行与结构回归 28 项通过；无新增差异仍执行测试与双评审回归 28 项通过。
- 前端相关 UI/API/store 回归 117 项通过，路径输入、迟到响应、模板切换和详情页分别完成聚焦验证。真实浏览器核对两模板入口、预检、项目默认文档目录、覆盖与正文、Mermaid、内部跳转和历史去重。
- 新库与 V126 升级至 V138 通过；跨 owner 非法旧候选拒绝升级，不丢弃历史证据。
- 0.4.71 首轮完整 Java 回归共 2330 项：28 个旧测试预期失败、2 个候选枚举夹具错误、3 个按条件跳过。失败报告保留。修正迁移数量/版本、工具枚举、私有角色权限与上下文依赖后，18 类共 114 项集中回归通过，随后分配 0.4.72 正式候选。
- 0.4.72 完整门禁通过：Java 2331 项，失败/错误 0、条件跳过 3；前端 97 文件/587 项通过。JAR 隔离启动、历史与预算恢复、文档包字节一致验证通过。但浏览器发现错误组件缺少导入，未更新正式服务。
- 补齐源码表单、详情、覆盖、文档及共享路径选择器的 ElAlert 导入；文档重读成功后清除旧提示。按生产组件注册方式复现旧版 2 项失败，再修正后完成 19 项界面回归。分配 0.4.73 重新执行完整门禁。

- 补充独立快照中断验收 3 项通过：清单已保存但正文部分写入时补齐同一快照；部分写入后源码漂移时阻断；正文已写全但未标记就绪时恢复原字节。使用当前编译类、独立 SQLite 和模拟 Provider，模型调用为 0；证据为 `snapshot-interruption-acceptance.json`。

## 真实模型证据

两项独立 Java 小样例均冻结当前配置 `opencode-go/deepseek-v4.1-flash`，原始源码、构建配置、已有测试哈希与全部试跑证据保留在本次隔离验收目录。

- 详细设计 run `928b14d6-a1dc-4a80-a8d9-b7ebaee5023e`：真实作者和独立复核各一次候选通过，2/2 文件 REVIEWED，8 篇 Markdown。API 正文、单篇下载、ZIP 与落盘文件逐字节一致，含未提交内容的源码保持不变。ZIP SHA-256 为 `55ca103d8e6f52a038bc8579fd859def7ff3cf93e8bb29abf4021ec2cba32df1`。重启后状态与制品保持。
- 首次单测 run `baa2abe5-49ae-447e-aa00-ce63d103a85f`：新增 17 个有效测试，保留原有 1 个，实际 Maven 测试共 18 项通过；但旧冻结的最终回归阶段误要求产生额外差异，两轮后进入 WAITING_INPUT。此运行不是成功，未到双 Judge，不改写冻结合同、不重置预算。新编译已修复该要求，仍保留范围、禁止删除、实际测试和独立评审。用户明确回复“允许，完成goal”，授权保留首次失败，沿用原模型与预算上限追加一次试跑。

## 0.4.73 完整门禁与 JAR

- `./scripts/verify.sh`：BUILD SUCCESS，20:39；Java 2331 项，失败 0、错误 0、跳过 3；前端 98 文件 / 590 项全部通过。
- 条件跳过保留：macOS 不执行 Windows CMD；未提供 `template.qualification` 的五轮完整模板资格测试；未提供 `loopper.cupxml2java.root` 的外部项目路由资格测试。真实试跑结果单独记录，不使用模拟结果替代。
- JAR：`/Users/wangyufeng/.codex/worktrees/source-template-tasks/opencode-loopper/target/opencode-loopper-0.4.73.jar`，351280731 字节，包含静态首页及 146 项 assets。
- SHA-256：`106998eecf93d7932a999ec7d3fef655b2cb6aa9154636976cfcbe36b5cc4406`。
- 隔离正式 JAR 在 8080 通过健康、版本、OpenCode 与私有 MCP、原试跑状态和冻结预算恢复；8 篇文档与原 ZIP 逐字节一致。浏览器确认待处理原因、恢复入口、设计正文、Mermaid 和文档内部跳转。范围预检的越界错误可见，修正输入后错误清除，预检不创建 run 或 Task。

## 本地服务更新

- 原 JVM 95310 及 owned OpenCode 95391 正常退出后，完整备份 234 个数据文件/链接并逐项核对 SHA-256；备份 SQLite `integrity_check=ok`，额外保存旧 launchd plist。
- 数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper-data-backups/53156-before-0.4.73-20260924-014011`。
- 沿用原地址、数据、JDK、配置及 launchd label，使用本版本已核对的正式启动器副本。新版本 0.4.73，JVM 85548，受管 OpenCode 85634，健康 UP、OpenCode AVAILABLE、内部 MCP CONNECTED。
- 运行 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.73.jar`，与构建 JAR 哈希一致。启动器保留在 `/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/launchers-0.4.73`，不依赖构建输出目录。
- 入口：`http://127.0.0.1:53156/template-tasks`。浏览器确认 7 个模板目录项、新输入字段及 6 条既有模板历史；本轮未发现 JavaScript 错误。
- 迁移后既有业务表计数、状态和项目/任务/知识库/PPT 历史摘要保持，数据库升至 V138，新源码模板表为空；原配置哈希保持。
- 证据：本次隔离 worktree 的 `data/source-template-qualification-20260924/`，包含 verify 日志、逐轮失败报告、JAR 摘要、隔离运行、浏览器、停止证明、备份清单与升级记录。临时运行数据和模型证据保留，不纳入源码提交。隔离 JVM 85424、OpenCode 85451 和 Vite 20772/20789 已证明退出，正式服务保持运行。

## 授权追加的真实单测试跑

用户明确授权一次修复后追加试跑。run `a894cffc-f798-4b44-b1db-c71d4fe9a4b9`、Task `b34ab4f4-63ba-4e15-8497-25e5cbc4bdcb` 从原始样例提交 `bfddc0258b3aeff72a2be6b28c9e299d99b3d83c` 开始，原模型、阶段 3 次/任务 12 次预算、超时配置与首次冻结合同逐项一致。

- 一次执行尝试完成，新增 `DeliveryFeeTest.java` 的 12 个真实行为测试，已有 1 个测试原样保留；聚焦和最后 `mvn test` 整体回归共 13 项通过，0 失败、错误或跳过。
- 四项正式验证全部 PASS；需求与风险 Judge 均 PASS，属于同一批 `7ae4c01e-b286-465f-a82d-66df52c9ab69` 和同一成功 Attempt。覆盖清单 1/1 TESTED，源码模板 COMPLETED，Task AWAITING_DECISION，产品内结果处置保持独立。
- 结果检查点 `9fd66ad64d6eccb62a01fd721640cfcab1b9ddb5` 相对基线只有新增测试；业务源码、构建配置及已有测试哈希在当前目录和结果检查点中均保持。工作区已由产品正常恢复至原分支，新增测试保存在待处置检查点。
- 原失败 run、冻结阶段、尝试记录与追加试跑前备份逐行一致；详细设计原 ZIP 未变化。浏览器显示“已完成”“测试已验证 1”和独立的“等待处置”提示。
- 证据保存在 `data/source-template-qualification-20260924/unit-fixed-acceptance.json`、`unit-fixed-evidence/`；首次失败证据继续保留。追加试跑使用的 JVM 96744 及 owned OpenCode 96775 已正常停止，数据库完整备份至同目录 `runtime-after-unit-success/`，完整性检查为 ok。

## 远端发布

分支 `codex/source-template-tasks` 和不可变标签 `v0.4.73` 已推送，标签指向验收提交 `1320ef8866a5f30f049274ecc85c0b23972e726e`。[Release Actions](https://github.com/wangyufengsky/opencode-loopper/actions/runs/35941859021) 全部成功：Windows 启动脚本通过；Ubuntu 完整门禁 29:56，Java 2331 项（0 失败/错误、3 条件跳过）、前端 98 文件/590 项通过。

[GitHub Release v0.4.73](https://github.com/wangyufengsky/opencode-loopper/releases/tag/v0.4.73) 已发布四项资产，全部重新下载核验大小、实际 SHA-256 和 GitHub 远端 digest；`SHA256SUMS` 对三项载荷的校验均通过。远端 JAR 的版本为 0.4.73，包含源码模板入口及 146 项静态 assets，两个启动脚本与已验证源码逐字节一致。

| 远端资产 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `opencode-loopper-0.4.73.jar` | 351280731 | `4ea87e4cf362487981f4337b7efd744c0a423e3cff059f4cbfb0ae10b96acfb1` |
| `SHA256SUMS` | 259 | `68034b8f2de8a3e2300cc53120080de86a95c6f04eb26d3771868e006c0e1ca6` |
| `start-linux.sh` | 15612 | `871406c952016c8a756f923a1feb918e7209b4168eb3108bbc77efdecaba7a24` |
| `start-windows.bat` | 17082 | `b9d41728df0aee2cde7329c59858b2ce41d2bb5a5ac560d5e121ebee4af72699` |

本地运行 JAR 与 Actions 重建 JAR 的哈希分别记录，不假定二者字节相同。远端证据为 `remote-release-acceptance.json`、`release-actions.log` 与 `remote-release-0.4.73/`。本次没有远端部署；Windows 全应用运行及两项额外条件资格矩阵未执行，条件跳过未折算为成功。
