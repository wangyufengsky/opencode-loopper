# 0.4.74 模板任务参数面板重新设计

## 范围

基于已发布的源码模板分支重新设计模板任务右侧参数面板。修复外层 Grid 拉伸导致短表单随左侧目录增高、标题和字段之间形成大块空白的问题；左右两栏按内容独立确定高度，左侧目录收紧，右侧使用模板说明、分区表单和底部操作。

单元测试开发与详细设计编写使用“所属项目 → 源码与输出 → 补充要求”的统一结构，分别呈现测试目录与文档目录、适用的产出说明和输入示例。处理范围检查、文件/模块/排除数量及输出位置就近显示，文件清单保留折叠。目录输入补充标签关联，配色复用现有皮肤变量，窄屏自然降为单栏。

预检、创建、正式开始、幂等和权限保持既有合同。创建成功仍进入详情页，由用户开始执行；输入改变仍使预检失效。本次未修改 Java 行为、数据库迁移、Provider 或产品任务执行逻辑。

## 验证

- 类型检查通过；模板页、源码字段与目录输入聚焦回归 13 项通过。
- 本机 Chrome 浏览器回归 12 项通过：三套皮肤 × 桌面 1440px / 窄屏 390px 的两模板布局与切换、预检和失效；旧模板项目路径继承、目录选择取消、分支选择、日期校验、目录搜索、需求上传重试、刷新和报告读取。
- 浏览器检查短表单不随七项目录拉伸、标签紧邻输入、窄屏无横向溢出；两源码模板切换后字段与预检状态正确。三套皮肤截图经检查。
- `./scripts/verify.sh`：BUILD SUCCESS，耗时 20:05；Java 2331 项，0 失败、0 错误、3 条件跳过；前端 98 文件 / 590 项全部通过。
- 正式 JAR 包含静态首页及 146 项 assets，已核对模板页编译资源包含新版分区和范围检查。

## 证据与边界

实施工作区：`/Users/wangyufeng/.codex/worktrees/source-template-tasks/opencode-loopper`，分支 `codex/source-template-tasks`。主工作区 `main` 保持原状。

证据目录：该工作区 `data/template-ui-20260924/`，包含三套皮肤截图、完整门禁日志、候选源码哈希与服务更新证据。旧 0.4.73 JAR 保留在 `artifacts-before-clean/`，活动服务 JAR 始终位于构建清理范围外。

此次为界面交付，浏览器自动化使用隔离响应夹具；真实服务仅核验加载、表单、预检和历史，不创建或启动真实模型任务。未执行真实 Provider 生成、Windows 全应用运行、远端发布或部署。

## JAR 与本地服务

- 构建 JAR：`/Users/wangyufeng/.codex/worktrees/source-template-tasks/opencode-loopper/target/opencode-loopper-0.4.74.jar`，351282656 字节。
- SHA-256：`11db91e87176d68c448818f17d9b40ba8d5a20316fc3d6bf1e16192a80830dae`。
- 运行 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.74.jar`，与构建 JAR 哈希一致。
- 原 JVM 85548 及其受管 OpenCode 85634 均已正常退出，停止证明已保存。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper-data-backups/53156-before-0.4.74-20260924-101901`；235 个文件/链接逐项核对一致，备份 SQLite 完整性检查为 ok。
- 沿用正式启动器、launchd label、JDK、端口与原数据配置。实际运行版本 0.4.74，JVM PID 22074，受管 OpenCode PID 22145。健康 UP、OpenCode AVAILABLE、内部 MCP CONNECTED。
- 地址：`http://127.0.0.1:53156/template-tasks`。升级前后既有表计数、业务状态及项目/任务/知识库/PPT/模板历史摘要保持，配置哈希一致；数据库仍为 V138。

## 实际浏览器检查

升级后重新加载正式服务，确认七个模板、新分区、两模板字段切换和项目文档目录继承。详细设计在已有演示项目的 `index.html` 上预检成功：1 个处理文件、1 个源码目录、0 项排除，创建入口随结果启用；修改输入立即清除旧预检。

边界仍由既有服务端控制：主 Loopper 项目预检命中可读源码 64 MiB 限制，演示项目 `.mjs` 文件被现有扩展名策略排除；两种情况均正确显示原因并禁止创建。未把它们计为成功预检，也未改变此次界面任务之外的来源策略。源码模板、Task、执行会话和文档模板记录数在预检前后保持一致，没有创建或启动模型任务。

历史入口在正式浏览器中继续显示原有 6 条模板任务。验收标签页已回到新版单元测试配置并保留，服务保持运行。
