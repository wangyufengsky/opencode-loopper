# 0.4.24：业务超时开关、JDBC URL 连接与 MCP 来源批量停用

## 交付范围

- 全局业务超时默认关闭。设置页可主动开启任务总时长、尝试及设计时限；任务、模板分析、文档开发、设计和模型评审沿对应冻结合同执行。开启时总时长最多 7 天，尝试与设计最多 24 小时。验证命令及连接等技术超时保留，人工取消和停止证明不变。
- 新运行明确保存开关。旧全局设置缺失字段默认关闭；历史任务、文档和设计按原冻结规则恢复，升级不重跑旧任务。V106 追加保存普通设计会话的开关和时长。
- 数据库页面改为 JDBC URL、用户名和独立密码。MySQL/openGauss 支持多节点地址，保留受控连接参数；内置 openGauss 驱动边界转换其接受的协议。旧连接编辑时转换，密码留空保留原加密引用，拒绝 URL 内嵌凭据及覆盖保护参数。
- 第三方 MCP 来源提供“关闭此来源全部工具”。完整目录、版本检查和整批事务回滚覆盖冲突；全局默认与项目覆盖保持独立，已冻结会话不追改，新发现工具仍默认停用。

## 验证

聚焦验证覆盖关闭时长达两天的任务／分析尝试、开启时的过期拦截、取消与停止确认、历史字段兼容、设计期限冻结、V105 → V106 升级、实际内置驱动接受多节点 URL、参数删除及凭据保护、来源批量操作的原子性。

旧续接超时测试已明确开启业务超时，不再依赖全局默认；历史迁移测试的最终版本期望更新为 106。聚焦续接集成测试 22 项通过。文档 HTTP 重启夹具补齐生产连接的 `transaction_mode=IMMEDIATE`，避免 SQLite 快照升级写锁冲突，聚焦重启及取消验证通过。完整前端测试 65 个文件、421 项全部通过。

浏览器验收使用构建后的前端及隔离 localhost 夹具：默认关闭和输入显隐、可读的时长输入、多节点 JDBC URL 与独立账号／密码表单、来源全部工具停用均已检查。未保存真实设置、未新增真实数据库连接、未调用真实 Provider，夹具进程已停止。

- 完整门禁：`./scripts/verify.sh` 成功（2026-09-14 16:06:29，11 分 41 秒）。后端 1912 项，0 失败、0 错误、3 条件跳过；前端 65 文件、421 项全部通过。跳过项分别依赖 Windows、外部 cupxml2java 项目和显式模板 Provider 资格开关。
- JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/target/opencode-loopper-0.4.24.jar`，300937082 字节；含 124 个静态资源条目、5 个内置 JDBC 二进制。
- SHA-256：`d7e93b6adb03cfcb9362a8bf733bb069734df661e7335976e9e23f336e9ca9da`。
- 隔离成品运行：默认开关 false；多节点 URL HTTP 保存／回读、删去参数不残留、密码留空保留、拒绝 URL 密码、加密凭据文件均通过。临时 JVM 56790 已退出，未连接真实数据库。
- 本地服务：正式 macOS 启动器已将 0.4.23 更新为 0.4.24，沿用 `http://127.0.0.1:53156`；新 JVM 56825，受管 OpenCode 56885，健康 UP、运行时 AVAILABLE、内部 MCP CONNECTED。旧 JVM 93932 与旧受管子进程 94008 均已退出。
- 运行 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.24.jar`，与成品哈希一致。
- 数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.24-20260914-160756`；启动脚本备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.24-20260914-160756-launchers`。
- 数据核对：1 项目、2 任务、24 会话、45 任务产物、2 报告包，数量和任务状态一致；数据库升级到 106，quick_check=ok；原保存启动配置哈希不变。两份报告分别 97125／131874 字节、29／9 条目，全部文件内容哈希与升级前一致。
- 实际浏览器：新版设置默认关闭，尝试时长输入隐藏；两条模板任务历史及原状态可见。未保存或改变真实业务配置。
- 运行收据：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/replacement-0.4.24.json`；门禁、失败排查日志及验收 JSON 保留于 `/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/acceptance-0.4.24`。


## 未覆盖边界

未连接用户内网数据库，未执行真实慢 Provider 长时间试跑。驱动加载和 URL 接受不能替代现场版本、认证和只读权限验收。macOS 已完成实际运行验证；Linux／Windows 仅覆盖启动器合同测试。本次不推送、不创建标签或远端 Release，不生成内置 JDK 平台包。
