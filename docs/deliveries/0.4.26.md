# 0.4.26 openGauss 驱动对齐与认证诊断

## 范围

- openGauss 新默认驱动对齐现场 IDE 的 `org.opengauss:opengauss-jdbc:7.0.0-RC3-og`，驱动类 `org.opengauss.Driver`，使用 `jdbc:opengauss:`。固定 JAR SHA-256 为 `a80bc5b50b8af012d8f99d20546885aa218673dab9c44aa64833abed65745a69`，按 profile 隔离加载；运行时不下载。
- 保留旧 `opengauss-6.0.3` profile、原二进制及 SHA、`org.postgresql.Driver` 和对应 URL。类型列表每个产品仅显示一个默认项；明确的历史 profile 仍可恢复，禁止跨产品替换身份。
- 已有连接在编辑抽屉中测试／保存时使用新默认驱动，并显示升级提示；测试不保存配置或密码，密码留空保留原加密引用。列表启停等元数据更新不升级驱动。已冻结 Task 保留旧配置，新 Task 使用保存后的版本；版本冲突仍拒绝。
- URL 的节点列表、IPv6 与白名单参数保持；用户与密码独立传递，包含空格、中文与特殊字符的密码不编码、不裁剪。
- SQLState 28 类认证错误返回 `DATABASE_AUTHENTICATION_FAILED` 中文指引，核对账号、目标节点和客户端驱动，不把认证拒绝等同于密码输错；其他未知异常继续安全隐藏。

## 验证

- 后端聚焦回归 26 项通过，包含新旧实际 JAR 的隔离加载、URL 接受、完整性和篡改防护、凭据传递、认证错误分类，以及持久化配置／任务冻结升级回归。日志 `/tmp/loopper-gauss-focused.log`。
- 前端类型检查和抽屉测试 3 项通过。日志 `/tmp/loopper-gauss-frontend.log`。
- Chrome 浏览器 1440px／390px 两项通过：旧连接升级提示、认证失败中文、草稿与保存载荷一致、保留密码、无页面异常。日志 `/tmp/loopper-gauss-browser-r3.log`，截图 `frontend/test-results/gauss-upgrade-1440.png` 和 `gauss-upgrade-390.png`。
- 首轮浏览器测试夹具错误匹配 `/src/api/client.ts`，导致空白页；修正为精确 API 前缀。第二轮在前轮退出前尝试启动导致端口占用，等待前轮退出后第三轮通过；失败证据保留于 `/tmp/loopper-gauss-browser-first-failure` 和对应日志。
- 完整 `./scripts/verify.sh` 通过，耗时 13 分 35 秒：后端 1,923 项、0 失败、0 错误、3 条件跳过；前端 65 文件、422 项通过，类型检查、静态构建、工具检查通过。日志 `/tmp/loopper-0.4.26-verify.log`。跳过项为 Windows CMD、未配置的外部 cupxml2java 仓库、五轮真实模板资格测试。
- 正式 JAR `target/opencode-loopper-0.4.26.jar` 为 302075412 字节，包含 126 个静态资源条目，ZIP 完整性检查通过。SHA-256：`a347ea89d92f7d2b553bcb6011b3d728198260f7933d11d72d3765b8868b3c3c`。
- 成品独立入口的本地 TCP 探针通过：新旧实际驱动均向 loopback 模拟服务器发送 3.51 握手，携带预期的合成用户名／数据库；模拟服务器返回 SQLState 28P01 后，两者均输出 `DATABASE_AUTHENTICATION_FAILED`。提取后二进制 SHA 与 JAR 资源一致。证据 `data/acceptance/opengauss-0.4.26-r3/summary.json`；测试未进入成功认证或查询阶段，不能替代真实数据库验收。
- 探针首轮受本机 JVM SOCKS 排除列表缺少回环地址影响；仅在探针进程补充 `socksNonProxyHosts` 后发现夹具只接受 PostgreSQL 3.0，需要按实际 openGauss 源码支持 3.50／3.51。修正探针后通过，产品和系统代理未修改。前两轮日志／报告保留于 `data/acceptance/opengauss-0.4.26/` 与 `opengauss-0.4.26-r2/`。
- 正式 macOS 启动器完成本地 `0.4.25 → 0.4.26` 替换：JVM `98138`，受管 OpenCode `98205`；原 JVM 77561 和原子进程 77632 已退出。健康 UP、运行时 AVAILABLE、内部 MCP CONNECTED。
- 访问地址保持 `http://127.0.0.1:53156`，运行 JAR 为 `/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.26.jar`，SHA 与成品一致。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.26-20260914-175345`；启动器备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.26-20260914-175345-launchers`。备份 quick_check=ok；任务、执行会话、制品与报告数量及任务状态保持，schema 106 不变，启动配置 SHA 不变，业务超时开关仍为关闭。
- 实际成品页面只读验收：openGauss 类型显示新驱动文件，取消未保存表单；历史任务页两条任务可见，浏览器错误日志为空。未创建数据库连接或调用 Provider。
- 替换收据：`data/test-services/0.4.15-20260912-191210/replacement-0.4.26.json`；启动日志：同目录 `startup-0.4.26.log`。

## 使用与边界

内网部署新 JAR 后，在数据库页面对已有 openGauss 连接选择“编辑”，确认显示 `opengauss-jdbc-7.0.0-RC3-og.jar`，使用与 IDE 一致的完整 URL、用户名和密码测试，成功后保存。仅在列表点击“测试连接”会测试原已保存配置，不执行升级。

本次未连接内网数据库，不能宣称现场 openGauss 9.2 认证问题已经解决；驱动对齐消除了已确认的客户端差异，真实认证与只读账号权限仍需现场验证。无远端推送、标签、Release 或内网部署。
