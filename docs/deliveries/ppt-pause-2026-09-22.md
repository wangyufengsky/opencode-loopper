# PPT 工作室暂停交接 — 2026-09-22

用户在 18:47 左右明确要求“暂时停止，我下班了，回家再做，你保存一下”。本次仅保存进度与安全停止临时工作，不视为完成交付。Goal 应为 PAUSED，恢复后继续当前目标，不重新设计或重做已通过的验收。

## 工作区与交付边界

- 仓库 `/Users/wangyufeng/IdeaProjects/opencode-loopper`，基线 `7928783` / `0.4.61`。开始时工作区干净，之前的皮肤改动已单独提交；本次源码均已落盘，尚未本地提交。
- 当前源码版本 `0.4.63`，版本引用已同步。远端 `v0.4.63` tag 查询为空、GitHub Release 返回 not found；恢复时重查。
- 全部实现阶段 P0–P4 已集成，仍需最终门禁、正式 JAR 隔离运行、正式服务备份替换、交付记录和本地提交。不得推送、创建 PR 或发布标签/Release。
- 三个子 Agent 已停止；运行时、引擎、前端分别完成原分工。所有构建继续由主 Agent 串行调度。
- 权威合同 `docs/ppt-contract.md`；实施账本 `docs/deliveries/ppt-development-plan.md`。

## 已有验证

- 0.4.62 首轮完整门禁：349 个后端报告、2196 项测试，11 failures / 0 errors；11 项均为新增 V119/V120 后旧测试写死最新版本118。七个对应迁移测试文件已统一改为120，保留历史升级起点及回退断言。
- 其后聚焦 `PptEngineTest` 与七个迁移测试：57 tests / 0 failures / 0 errors。含三类原生图表 XMLBeans Schema 校验；折线图必需 grouping 已补 `STANDARD`。
- 0.4.63 最终 `./scripts/verify.sh` 在用户暂停时中断。已执行部分不能替代完整通过，恢复后重跑；本次中断不是业务测试失败。无需因单纯暂停重试再升版本，若可执行内容改变按公约重新判断版本。
- 早先受影响回归：82/58 等分批检查及运行时/权限/知识/MCP/结构回归记录见 `/tmp/ppt-*.log`；最新工作区11项、Agent12项、引擎21项均通过。
- 前端：18 项 PPT Vitest、4 项 Playwright；原生 Chrome 实测七模块自动保存/刷新、两次确认、手工制作、对象方向键/拖动/缩放、PNG版本刷新与实际 PPTX 下载。
- REST 12页中文样本：文字、图形、图片、表格、三类图表、分组、备注；三轮修改后无关页面保持、锁定封面 PNG 完全相同、历史 PPTX 哈希不变。
- 真实模型 `opencode-go/deepseek-v4.1-flash`：完整规划→确认→两页制作；另一个同 Session run 完成故意溢出→测量→修复→检查→PNG/PPTX作业完成。PPT角色和11个私有MCP工具真实使用，持久化scope凭证匹配数0。
- 真实重启：旧WAITING_INPUT run经精确受管进程退出证明转STOPPED，原问题和revision保留；重启后的新run已COMPLETED并保存revision1。

## 证据与样本位置

- 主证据根：`data/ppt-dev-runtime-20260922/`。
- `model-second-run-evidence.json`：同会话纠错/导出，11次工具。
- `model-workflow-plan-evidence.json`、`model-workflow-production-evidence.json`、`workflow-real.json`：真实规划/两页制作；制作run的工具时序止于预览，不将后续REST导出误归因给该run。
- `restart-evidence.json`、`model-restart-resume-evidence.json`：重启与新run续写。
- `frontend-evidence.md`：真实Chrome坐标、版本、下载哈希。
- `office-ui-evidence.json`：WPS实际打开12页、原生图表选择、标题文字编辑保存并XML确认；PowerPoint只读打开证据。
- `native-roundtrip/native-roundtrip-evidence.json`、`sample-native-edited.pptx`、`NativePptRoundTrip.java`、`execution.log`：独立POI重读后编辑文字/shape/表格/三类原生chart及嵌入xlsx再读通过；55个非目标对象XML、12页notes、图片字节及其他单元格保持。该项不等同WPS UI图表数据编辑。
- 最新12页REST样本：`data/ppt-qualification/2026-09-22T10-07-54.474Z/`，`sample-revised.pptx` SHA-256 `15390949f412bf5f1f16cdce72b27456dfb37013e4c28b5c5501317073fd1c94`。这是修复图表轴之后、折线grouping补强之前的旧样本；正式JAR完成后需重新生成最终样本。
- `verify-0.4.62/reports.tar.gz`、`summary.json`保留第一轮失败报告。
- `/tmp/ppt-final-format-migration-focus.log`为57项通过；`/tmp/ppt-0.4.63-verify.log`为暂停的完整门禁日志。暂停时会将日志和工作区源码快照另存 `data/ppt-pause-20260922/`。

## 办公软件验证边界

WPS 的原生文字编辑已实测保存，柱状图首尾裁切已修复复验。通过“编辑数据”“选择数据”未获得可操作的工作簿窗口；关闭重开和显式autoUpdate=0诊断副本表现相同，尚不能宣称WPS UI修改图表数据通过。独立POI工作簿编辑回读通过，但需要保持证据分层。未激活的本机PowerPoint提示必须订阅才能编辑保存，仅完成打开、中文与备注显示；没有进行激活。真实内网Provider、Windows未覆盖。

## 正式服务（暂停期间保留）

- 原服务仍为0.4.61，URL `http://127.0.0.1:53156/`。
- JVM PID61628，owned OpenCode PID61691，OpenCode端口63546，型号 `opencode-go/deepseek-v4.1-flash`；恢复后必须重新核实PID身份，不能盲用旧PID。
- 服务目录 `data/test-services/0.4.15-20260912-191210/`；活动JAR `opencode-loopper-0.4.61.jar`，数据子目录 `data/`，旧JAR不在Maven clean范围。
- 原JAR SHA-256 `12f9eff364aa82e782ffc9c3d2e780d4382d442b64cf948aa6b62bc3021777b9`。
- JDK `/Users/wangyufeng/Library/Java/JavaVirtualMachines/temurin-21.0.12/Contents/Home`；保存设置在数据下 `config/startup-overrides.properties`。
- 停止前预检查：7 Task（3 COMPLETED、2 AWAITING_DECISION、1 CANCELLED、1 WAITING_INPUT），3 Project、2 Designer（CANCELLED、REVIEWING/CANDIDATE_ACCEPTED）、10 Knowledge（全IDLE）；所有execution_session终态，持有lease的WAITING_INPUT任务writer_session_id为空。受管远端 `/session/status` 返回0活动会话。正式替换前须重查。
- 本次临时开发JVM78045与其owned OpenCode78113、Vite71152、最终Maven96154/测试JVM97008按用户暂停请求正常终止；精确停止结果保存在暂停目录。正式61628/61691未发停止信号。

## 恢复后的下一步

1. 读取本交接、根/src/frontend公约、git status，确认没有新的用户改动和运行进程；Goal恢复后继续。
2. 重新执行0.4.63完整 `./scripts/verify.sh`，串行构建。若失败集中定位/修复，保留日志；已有通过的真实模型和浏览器验收只在相关代码改变时重做。
3. 通过后验证JAR、静态资源、Noto字体与OFL许可、SHA-256。独立数据和端口启动正式JAR，以空PATH+绝对JDK/headless模式验证无需Node/Python/Office生成PNG/PPTX；用 `scripts/qualify-ppt-workspace.mjs` 生成最终12页样本和三轮修改。
4. 用最终文件核验原生结构及WPS；记录尚未覆盖的UI图表编辑/办公软件边界，不能伪称通过。
5. 按 `docs/development.md` 正常停止已确认归属的旧正式服务和owned子进程，停止后完整备份数据到数据目录外；校验备份。保留配置，经正式 `scripts/start-macos.command` 启动新JAR，沿用53156地址和原数据。
6. 验证实际版本/PID/健康/内部MCP/历史保持/真实PPT页面，保留正式服务，清理仅本次临时进程。
7. 写最终交付记录、更新账本，检查diff/暂存区，只创建本任务本地提交。适用验收与交付完成后才将Goal设complete。
