# 0.3.93：恢复独立 JAR 发布流程

## 变更

- 根据用户要求，将 `.github/workflows/release.yml` 与 `scripts/verify.sh` 恢复至六包改动前的内容，逐字核对与 `35aae24` 一致。
- Release 保留 Windows 脚本验证、版本与标签匹配检查、完整 Maven 门禁，发布独立 JAR、Linux/Windows 启动脚本及 SHA256SUMS。
- 默认本地验证不再调用平台包组装工具。六包工具、JDK 锁文件及自定位启动能力保留为手动可选功能。
- README、开发公约和开发文档同步为默认 JAR 交付，版本引用同步至 0.3.93。

## 验证与产物

- `./scripts/dev-check.sh backend ReleasePackagingContractTest,BundledLauncherTest`：11 项通过。
- `./scripts/verify.sh`：BUILD SUCCESS，耗时 7 分 21 秒。Java 1563 项，0 失败、0 错误、2 条件跳过；Vitest 43 文件、315 项通过；工具测试 6 项与统计守卫测试 7 项通过。
- 从恢复后的 workflow 提取并执行 Prepare release assets 步骤，生成 `target/release/` 中的 JAR、两个启动脚本及 SHA256SUMS；`sha256sum -c SHA256SUMS` 三项全部通过。
- 正式 JAR：`target/opencode-loopper-0.3.93.jar`。
- JAR SHA-256：`9aff20e256ed1fbf0631d8fff2912cbf8aaed734e9ac2e7c6c2ecb882b0ef673`。
- JAR 包含 Spring Boot JarLauncher、`BOOT-INF/classes/static/index.html` 及 112 个 JS/CSS 资源。
- 日志与产物核验记录位于 `target/workflow-restore-evidence/`。
- 为避免 Maven clean 删除原有产物和历史证据，原 target 整体保留在 `/var/folders/dd/rj49_gmj4db8m9vpzws8fkf00000gn/T/loopper-workflow-restore-k3bepb9x/target-before-0.3.93`。

## 交付边界

本次仅本地提交；未推送、未创建标签或 Release、未部署或重启既有服务。未执行远端 Actions、Windows 原生脚本验证或新 JAR 的实际服务启动；本次未下载分发用 JDK，也未生成六个平台包。
