# 0.4.21 数据库、工具目录与模板进度交付

本批修复工具重复展示，将数据库管理改为表格和分区侧栏，并增加内置驱动自动匹配、草稿连接测试及服务端筛选。模板进度采用服务端步骤投影、分析批次环形比例与分类进度条；会话标题使用实际批次关联，支持人员贡献全局序号和返修轮次。

数据库新建类型为 MySQL、openGauss、达梦。固定驱动版本、依赖和 SHA 随完整 JAR 的 jdbc-bundled/NOTICE.md 交付；GaussDB 和 GoldenDB 仅保留历史配置读取、停用、归档及冻结任务恢复。没有真实数据库产品实例，本批不宣称现场数据库兼容性验收通过。

已完成的聚焦检查：

- TaskSessionMonitorServiceTest：3 项通过。
- 数据库、只读 SQL、内置驱动和进度投影等首轮 59 项中，58 项通过，1 项新增测试夹具缺少人员信息；修正夹具后与任务执行、会话、结构检查共同回归 43 项全通过。草稿无持久化、版本游标和历史配置边界最终 6 项集成测试通过。
- 6 份前端聚焦测试共 13 项通过；类型检查通过，工具目录及侧栏调整后追加 4 项通过。
- Playwright 独立端口、API 夹具验收：1440px／390px 两项通过，覆盖数据库侧栏、测试结果失效、环形进度和“阶段 2 · 分析批次 12/14”标题；没有调用真实 Provider。浏览器检查发现 Drawer 缺少按需注册，已修复并复跑通过。
- 内置驱动冷提取、重启复用、驱动类／URL、依赖隔离、篡改与符号链接拒绝通过；不等同服务器连通或账号只读权限实测。

受限沙箱的 Mockito 附加 JVM 初始化失败输出保留于本次验收日志，允许测试进程权限后复跑通过；没有更改测试语义来规避失败。

候选 0.4.20 的第一轮完整门禁：后端 1,784 项，失败 0、错误 0、跳过 3；前端 397 项通过，但两份文件因组件内 CSS 导入未能加载。Drawer 样式移到统一 main.ts 入口后复跑，最终候选改为 0.4.21。最终 `./scripts/verify.sh` 成功：后端 1,784 项，失败 0、错误 0、跳过 3；前端 56 份文件、400 项全通过。跳过项为显式真实 Provider 模板资格、Windows cmd 专项和依赖外部真实 Maven 项目的路由夹具，本批不将这些计为通过。本批仅本地提交与 JAR，不发布远端、不替换正在运行的 0.4.19 服务。

## 成品与运行时证据

- 成品：`target/opencode-loopper-0.4.21.jar`，300,368,219 字节。
- SHA-256：`7f4967cce5dcd566c0c200b8c4ac97f8fb05b39a829f804ca9a1ba1ceeb13a0a`；校验文件 `target/SHA256SUMS`。
- 已确认正式静态入口引用的 JS／CSS 均存在；5 个内置驱动／依赖资源逐项匹配固定 SHA，厂商驱动不在 BOOT-INF/lib 应用类路径。
- 使用原数据库的 SQLite 只读备份，在独立目录与随机 loopback 端口启动成品；关闭调度和恢复自动动作，使用 fake 模式，不调用真实 Provider。macOS 沙箱禁止外网，页面拦截与计数确认外网资源请求为 0。
- 三组驱动在空驱动缓存中由成品提取并验 SHA；关闭端口的只读探针明确返回 BLOCKED，不伪装为真实数据库连接通过。
- 实际 REST 与浏览器核对历史周报：14 个会话关联全局 1–14 批次，代码 10／10、人员 4／4；选中第 12 批显示人员贡献第 2／4 批。数据库自动匹配保存、中文查询筛选、侧栏和辅助工具单行展示均通过。
- 任务 2、执行会话 24、制品 45、模板任务 2 的数量及原任务状态在隔离验收前后完全一致，SQLite integrity_check 为 ok。验收进程已正常关闭。
- 原服务仍为 0.4.19、AVAILABLE，未重启、替换或写入其数据库。
- 本地验收记录、截图和完整日志位于 `data/acceptance-0.4.21-ui/`；其中保留首轮构建失败及验收脚本的沙箱语法／中文 URL 编码修正过程。后续脚本修改未改变成品。
