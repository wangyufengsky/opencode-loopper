# 0.4.23 模板任务第二期交付

状态：完整门禁、正式 JAR、本地运行及历史保留核验通过；本地提交号随最终交付回复提供。

## 交付范围

新增需求开发和需求代码评审，共用不可变多文档、分段原文、带来源需求清单、独立复核及报告。上传产生独立发起记录；单包/滚动设计有效后才关联执行 Task。用户从项目入口继承项目，全局入口选择项目。

需求开发复用当前目录、任务分支、正式 Start、目录租约、验证器和同批双 Judge。范围内自动推进，业务问题等待用户；最终跨包回归进入计划及验收。执行成功保留独立结果处置。补充文档创建新需求版本，保留已执行事实；安全剩余包重规划以明确停止证明和冻结来源为前提。

需求代码评审只读所选分支的冻结 SHA，按功能及代码链路分析，不遍历全部历史、不运行项目构建或测试。逐项区分符合、部分实现、实现不符、未实现、无法判断；问题区分缺陷、验证缺口和建议。报告完成与需求全部满足分别呈现。

前端提供上传、稳定详情、原文/需求/报告分页和按需读取、业务回答及补充上传。统一列表按模板来源筛选并去重，刷新及 SSE 断线以 REST 恢复。新增 V84–V105 迁移，历史迁移保持不变。

规范见[文档模板任务合同](../document-template-contract.md)，逐轮过程见[实施账本](template-phase2-plan.md)。

## 验证层次

- 可控 Provider 整链：文档提取及独立复核、静态评审及结论复核、单包、滚动两包、业务等待、补充文档及剩余包重规划、取消和恢复。
- 真实执行证据：隔离 Git/非 Git 项目的当前目录开发、离线 Maven/JUnit、最终回归；破坏前包的负例被最终回归拒绝，修复后的新包经双评审生成报告，Task 保持等待结果处置。
- 人工标注样本：遗漏、无依据新增及错误合并的反馈修正协议；这是分析合同证据，不是自然语言理解准确率测评。
- 隔离 HTTP：随机端口与独立文件 SQLite，两次应用生命周期验证上传、原文读取、同键重试和取消；执行中 Task 另用正式重启恢复入口验证。两者不冒充真实 Provider 或跨 JVM 全流程试跑。
- 浏览器夹具：本机 Chrome 1440px/390px，上传响应丢失、相同幂等键重试、刷新、按需报告及下载各通过。
- 首次完整门禁失败已保留。修正旧迁移断言、新角色枚举断言、可选身份校验依赖，并将测试代理限定到测试 JVM 后，177 项兼容聚焦回归通过。

## 最终门禁与运行

完整命令：`MAVEN_ARGS='-DargLine=-javaagent:/Users/wangyufeng/.m2/repository/org/mockito/mockito-core/5.23.0/mockito-core-5.23.0.jar' ./scripts/verify.sh`。2026-09-14 14:44:30 完成，耗时 11 分 13 秒，BUILD SUCCESS。代理仅用于本机测试 JVM。

- Java：1,901 项，0 失败、0 错误、3 条件跳过。
- 前端：65 个文件、420 项测试全部通过；TypeScript、正式 Vite 构建通过。
- 脚本测试：发布版本检查 6 项、统计门禁 7 项通过；项目合同检查通过。暂存后对新增文件的 `git diff --cached --check` 发现 V98 迁移末尾一处多余空行；该迁移已经在实际升级中执行，按迁移不可修改约束保留原字节。其余文件空白检查通过，该空行不影响 SQL 行为。
- 跳过：Windows CMD 专属用例；缺少外部 `loopper.cupxml2java.root` 的路由样本；需要 `template.qualification` 明确开启的真实模板效果资格试跑。
- 日志：`/private/tmp/loopper-phase2-0.4.23-verify-r2.log`。首次失败日志及测试报告另行保留，未替换为成功记录。

正式产物：`target/opencode-loopper-0.4.23.jar`，内含 124 个静态资源文件和新增页面，Manifest 版本为 0.4.23。

SHA-256：`1c73582f757d1ad8cbe49e7b004b9cc9fbafa6212e7fb25a36410a229abff28d`。

启动前通过进程及端口确认没有原 Loopper JVM，原端口空闲。按可核实的上次运行配置，通过复制后的 `start-macos.command` → `start-linux.sh` 正式启动，无需停止旧进程。

- 地址：`http://127.0.0.1:53156`；主 JVM PID `93932`，OpenCode 托管子进程 PID `94008`。
- 活动 JAR：`data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.23.jar`，哈希与正式产物一致。
- 数据：`data/test-services/0.4.15-20260912-191210/data`，V83 → V105 升级完成，SQLite quick_check 为 ok。
- 备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.23-20260914-141843`，完整数据及旧启动器保留，备份数据库 quick_check 为 ok；旧 0.4.22 JAR 保留。
- 健康 UP，运行接口版本 0.4.23，OpenCode AVAILABLE、内部 MCP CONNECTED；已保存启动配置与备份字节一致。
- 历史保持：1 个项目、2 个任务、24 个执行会话、45 个制品、2 个模板报告包；任务状态未变化。两份历史报告 ZIP 分别含 29/9 个条目，完整性校验通过。
- 新目录包含需求开发、需求代码评审及第一期两个模板。验收未创建文档任务，未触发真实 Provider 调用。
- 活动 JAR 的本机 Chrome 只读验收通过：需求开发无分支输入，需求代码评审含分支且无日期输入；上传表单与任务历史加载，页面异常为 0。截图 `/private/tmp/loopper-0.4.23-{development,review,history}-live.png`。
- 运行记录：`data/test-services/0.4.15-20260912-191210/replacement-0.4.23.json`；启动身份及日志保存在同目录。主进程脱离工具会话持续运行。

| Goal | 对应完成证据 |
| --- | --- |
| G1 | 多文档冻结、分段/来源/覆盖校验、标注样本修正协议及独立复核集成 |
| G2 | Git/非 Git 当前目录单包、滚动包、业务等待、补充重规划、真实测试及双评审/最终回归正反例 |
| G3 | 冻结代码树、角色读取权限、静态整链及结论/报告/ZIP 校验 |
| G4 | 幂等上传、统一列表、按需详情、两次 HTTP 应用生命周期、前端及 Chrome 验收 |
| G5 | 完整后端/前端回归、历史迁移、旧任务与报告在活动服务可读 |
| G6 | 完整 JAR 门禁、哈希、原配置启动、数据保留、运行记录及本地提交 |


## 未执行与延期

未调用真实 Provider 做效果试跑；因此不宣称真实模型提取、需求识别或评审准确率验收通过。未执行 Windows 专属运行测试或外部示例仓库专属测试；完整门禁的条件跳过项在最终证据中分别记录。

旧 DOC 转换、OCR、评审运行测试、自动修复、多仓库、全仓库质量扫描和自动合并按批准范围延期。本次仅本地交付，不推送、打标签或创建 Release。
