# 0.4.30：设置页与需求文档列表重设计（本地交付）

## 范围

- 设置页改为六个分区导航与单面板布局，统一标题、间距、控件高度、开关行和生效提示。两项字段采用两列，多列控件按底部对齐；窄屏导航与表单自然降列。
- 分区切换保留未保存编辑；保存继续提交完整配置，路径、命令行及模型校验失败时展示对应面板。修正命令行路径错误原先出现在项目根字段下的问题。
- 模板任务与补充文档共用文件选择组件：统一选择按钮，显示数量、总大小、文件名、类型和大小，可逐项移除；取消重选保留原列表，提交中禁止选择和移除。原有格式、单文件和总量校验保持。
- 上传完成后的“文档与提取局限”和“原文目录”共用文件摘要卡片，展示完整文件名、分段数、提取局限数量及展开指示。原文仍按需读取，不新增任务或模型调用。
- 本次没有后端业务代码、API 或数据库迁移变更。版本引用按正式 JAR 门禁同步为 0.4.30。

## 聚焦验证与视觉验收

- `./scripts/dev-check.sh frontend src/views/SettingsView.spec.ts`：类型检查及设置页 5 项回归通过。
- 六个相关测试文件共 16 项通过：设置、模板入口、补充文档、文件选择组件、原文目录、需求任务详情。覆盖完整保存、错误分区、未保存编辑、选择取消、替换和移除、提交禁用、非法文件拒绝、原文延迟读取与旧响应丢弃。
- 浏览器检查正式构建的静态资源，六个分区在 1280、1024、768、390 像素宽度下均可显示，24 个组合均无页面横向溢出；界面控制台未发现错误。
- 选择 DOCX 和长文件名 Markdown，确认原生按钮已隐藏、数量和大小正确、逐项移除更新列表。上传完成后的文件摘要使用独立 HTTP 模拟记录验证长名称、提取局限展开及窄屏换行；这是界面证据，不是真实需求任务执行证明。
- 临时视觉验收服务仅监听本机，不提供写操作；本次未保存或更改活动服务的业务设置，未创建真实需求任务，未调用真实 Provider。

## 发布范围

用户明确要求“做完暂时不发布新版本”。本次仅完成本地 JAR、服务更新及本地提交；不推送分支、不创建标签、不发布 GitHub Release。

## 完整门禁与本地服务

`./scripts/verify.sh` 于 2026-09-15 11:35:38 完成，用时 14 分 52 秒。后端 1,958 项，0 失败、0 错误、3 项环境条件跳过；前端 68 个文件、430 项通过，项目与工具检查通过。完整日志：`/private/tmp/loopper-030-verify.log`。环境条件跳过不计为验证通过，真实 Provider 与远端发布不在本次验收范围。

正式 JAR：`target/opencode-loopper-0.4.30.jar`，302,193,142 字节，128 个内置静态文件；ZIP 完整，包含新版设置页和文件选择组件。SHA-256：`1782df2a215faaf1d9b4f0b02fc201d4357a3c5927909b6b74924ec9d1f6cfbb`。产物凭据：`data/release-verification/0.4.30/local-artifact.json`。

本地服务通过正式 macOS 启动器完成替换：旧 JVM 36686 和受管 OpenCode 36743 均确认退出，新 JVM 58571。服务地址保持 `http://127.0.0.1:53156`，实际版本 0.4.30、健康 UP、Runtime AVAILABLE、内部 MCP CONNECTED。运行 JAR：`data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.30.jar`，与正式 JAR 哈希相同。

完整数据备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.30-20260915-113652`；原启动器另行备份到同名 `-launchers` 目录。替换前后完整设置响应哈希及启动配置哈希一致；1 个项目、2 个任务、24 个完成会话、45 个任务制品、2 个报告包及任务状态保持。数据库仍为 schema 109，`quick_check=ok`。运行回执：`data/test-services/0.4.15-20260912-191210/replacement-0.4.30.json`。

替换后浏览器确认实际服务的两个历史任务仍可见，新版设置分区及模型字段正常加载；临时前端开发服务和静态验收服务均已停止，交付服务保持运行。
