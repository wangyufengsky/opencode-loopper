# 0.4.22：第二批辅助 MCP

## 本次交付

GitLab 沿用单实例环境变量配置和本地 REST 适配，增加项目仓库绑定、origin 候选识别、保存后连接检查，以及 9 个只读工具：项目背景、Issue、MR、讨论、差异、Pipeline、Job 和日志。读取结果保存来源快照，分页、内容过大、接口兼容和权限错误明确返回。页面不存储 Token，不新增 GitLab 写操作。

日志与测试证据接入正式验证执行，保存合并进程输出、JUnit 报告和登记的应用日志增量；记录任务、阶段、尝试、执行标识、时间、摘要与完整性。新增失败用例列表、失败详情、字面检索工具和任务页面，扩展现有证据读取工具。JUnit 解析结果用于诊断，不修改原验证器或 Judge 判定。

V83 追加项目配置、资源冻结、证据快照和解析缓存。新增 12 个工具默认关闭；旧任务不追授资源。复用 JVM、SQLite 和制品目录，无新常驻平台、启动扫描或索引。路径、读取量、搜索时间、输出、任务磁盘配额均有限制。配置和使用合同见 [辅助 MCP 合同](../assist-mcp-contract.md)。

## 验证证据

- 最终 `./scripts/verify.sh` 通过；Java 1,794 项，0 失败、0 错误、3 项跳过；前端 58 个文件、404 项通过；项目与工具检查通过。
- 聚焦回归包括 GitLab 模拟 HTTP 传输、编码路径、分页、差异兼容、重定向、错误与大小限制，原 MR 查询；证据 XML 安全与编码、脱敏、路径、范围、配额、磁盘失败、权限时间边界，以及 V82 → V83 升级。最终完整门禁修复前的失败日志保留于 `/tmp/loopper-0.4.22-verify.log`，最终日志为 `/tmp/loopper-0.4.22-verify-final.log`。
- 独立数据升级：原有 2 个任务、24 个执行会话、45 条任务制品记录、1 个项目保持；数据库完整性检查通过。使用 Fake Provider 和本机合成 GitLab，无真实 Provider 调用。
- 通过正式 API 创建隔离验收任务并执行失败命令，取得 8 条快照、3 条按各执行边界保存的失败记录；失败异常栈、来源与搜索命中正确。报告在后续验证边界未改变时明确标记来源未确认。
- 浏览器确认项目绑定、来源规则、保存操作、任务快照目录、失败详情及搜索命中；相关页面未捕获浏览器错误。
- 同机独立数据、Fake Provider、正式启动器的一次启动观察：0.4.21 就绪 5.00 秒、RSS 392,800 KiB；0.4.22 就绪 4.93 秒、RSS 443,552 KiB。这是启动瞬间单次采样，不能据此推断稳定内存或性能改善。未加入启动日志扫描和额外后台平台。
- 独立验收记录：`/tmp/loopper-0.4.22-isolated-20260913-135147/acceptance.json`。模拟通过不等于真实内网 GitLab 联调通过；真实地址和凭证留待用户现场验收。

## 完整产物

- JAR：`target/opencode-loopper-0.4.22.jar`，300,446,341 字节。
- SHA-256：`de1521252ca628af624e989b9854473bb6c52f0e18f2cc63acaaf9450d21a5c3`。
- 已检查 JAR 内正式静态首页、前端资源、V83 迁移和新增接口类。

## 当前服务替换

用户已明确授权在隔离验收、本地提交后替换当前服务，保留原地址 `http://127.0.0.1:53156`。替换已完成；未执行远端推送、标签或 Release。


- 实现提交：`510edf1`。
- 旧 JVM `61221`、受管 OpenCode `61271` 均已退出；优雅关闭因 MCP 长连接等待约 30 秒，首次脚本在等待期限停下，核实两者退出后续接备份。未强制终止或在停止不明时启动新 writer。
- 停止后完整数据备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.22-20260913-142023/`。数据库完整性与记录数量检查通过；旧 JAR 保留。
- 新 JVM `96938`、受管 OpenCode `96988`；正式启动器使用原数据目录、53156 端口及已保存设置。健康 UP、运行版本 0.4.22、OpenCode AVAILABLE、内部 MCP CONNECTED；实际启动至就绪约 5.96 秒。
- 设置表内容摘要相同，模型仍为 `opencode-go/deepseek-v4.1-flash`；任务 2、执行会话 24、任务制品 45、项目 1、模板报告包 2，替换前后相同。
- 实际运行 JAR：`data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.22.jar`，SHA-256 与正式产物相同。
- 回执：`data/test-services/0.4.15-20260912-191210/replacement-0.4.22-20260913-142023.json`。
- 原地址浏览器确认新配置入口、原两个任务分别已完成和等待输入，以及历史任务“暂无已采集快照”提示。真实数据未注入验收夹具，GitLab 环境变量凭证目前未配置。
- 隔离 JVM 和模拟 GitLab 进程已停止，保留隔离验收数据及日志。

验收路径：项目页“GitLab 与证据”保存仓库绑定并检查连接 → “工具与 Skill”按需启用辅助 MCP 工具 → 登记报告及日志来源 → 创建新任务检查 GitLab 只读查询、失败用例与证据。旧任务不追授新资源。
