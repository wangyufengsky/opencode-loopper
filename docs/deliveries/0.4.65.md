# 0.4.65 — 知识库引用、对话展示与 PPT 活动记录

知识库提示统一改为先用已授权 MCP 查询并读取原文，引用使用实际 citationId；只有 MCP 查不到、来源不支持或不可用的部分才使用项目范围内原生只读调查。保留冻结来源、权限和正常回答后不自动追加调查的行为。

答案中的本地文件链接改为在右侧打开文件预览，使用对话冻结来源及原有文件安全检查，支持行号定位与分段继续读取。当前文件明确区别于回答时保存的编号证据，不再跳转到不存在的静态 URL。

保存模型设置后立即刷新共享运行状态，左下角随之更新。知识库用户消息增加可见气泡和蓝发鲸鱼娘风格的生成头像；参考网络流传的 DeepSeek 同人形象重新绘制，不使用真人照片。

PPT 与知识库共用思考、等待和工具调用展示组件，思考与工具独立折叠，流式刷新保持展开状态。PPT 保存 Provider 实际公开的思考和工具状态，不推测思考、不保存工具授权或原始参数；活动失败不影响业务，旧轮次结果不能覆盖新轮次。追加 V122，保留旧对话、作品及生命周期。

## 验证

- 聚焦回归覆盖 MCP 提示与传输、文件预览权限、PPT 活动采集、问答恢复、旧轮次拒绝覆盖、迁移及前端展示。首轮 153 项中的两个失败分别是旧提示断言和新测试缺少停止确认轮询，修正后受影响 26 项全部通过；补充思考长度边界后 14 项 PPT Agent 回归通过。失败日志保留。
- 前端类型检查、组件与 Markdown 链接测试通过。两条新增 Playwright 用例单独重跑，退出码 0：文件在当前页预览、不可读时显示原因、移动端气泡头像无溢出；PPT 思考与工具独立展开且更新后保持。较早的 27 条浏览器用例断言全部通过，但测试进程退出清理挂起，人工中断后退出码 130，不将其当作正常进程退出证据。
- `./scripts/verify.sh` 于 2026-09-22 22:21:08 通过，用时 18 分 46 秒：Java 2,226 项，零失败/错误、3 项按环境条件跳过；前端 89 个文件、551 项全部通过。构建工具和运行防护检查通过。
- 跳过项为显式开启的模板资格评测、真实 Windows cmd 启动、需要外部 CupXml2Java 路径的资格样本；不视为实测通过。
- 正式 JAR 的隔离真实模型 `opencode-go/deepseek-v4.1-flash`：知识库完成 11 次 MCP 调用、0 次原生读取，保存 2 条引用，回答正确给出普通/高级付款上限及冻结规则；原文件链接和编号引用均经浏览器点击打开，目录越界请求拒绝，样本文件保持。PPT 完成只读上下文查询，保存 293 字符的实际思考及 2 次成功工具调用，正式页面可以展开查看。此 PPT 验收聚焦活动采集展示，不冒充新的完整成稿导出验收。
- 真实模型验收的临时 JVM 及其受管 OpenCode 均确认退出；数据独立于交付服务。

## JAR 与本地服务

- 成品：`target/opencode-loopper-0.4.65.jar`，350,845,527 字节。
- SHA-256：`d6547c71e6f4285b255fde52046278307037255df2d963f3dd48c5f543cf1dfe`。
- 核验 JAR 中的鲸鱼娘 PNG、文件预览类及 V122；构建后源码哈希保持。
- 原 JVM 50528 及其受管 OpenCode 50613 均正常退出，才启动新服务。完整数据备份的 216 个文件逐项核对 SHA-256，SQLite `quick_check=ok`。
- 备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.65-20260922-222337/data`。
- 通过官方 macOS 启动器及原 launchd 服务启动，沿用原数据目录、端口、JDK 和保存设置。实际版本 0.4.65，JVM PID 74959，受管 OpenCode PID 75035；健康 `UP`、运行环境 `AVAILABLE`、内部 MCP `CONNECTED`。
- 访问地址：<http://127.0.0.1:53156/knowledge>；运行副本 `data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.65.jar` 与成品哈希相同。
- 升级前后 3 个项目、7 个任务、80 个执行会话、10 个知识对话、17 条知识问答、92 条引用、3 份 PPT、37 个作品版本、51 个制品均保持；知识与 PPT 历史表内容哈希、保存设置哈希均相等。迁移从 V121 到 V122。
- 更新后原地址的真实浏览器通过知识库入口、侧栏运行信息、PPT 历史和消息兼容检查，零页面错误、零业务写入。模型切换另以正式页面配合 API fixture 验证，避免改变用户保存设置。

完整日志、备份清单、JAR 校验、浏览器截图及真实 Provider 证据在 `data/chat-delivery-0.4.65-20260922-220043/`。交付服务保持运行；只创建本地提交，不推送、不创建 PR、标签或 Release。

头像参考网络流传的蓝发鲸鱼娘同人特征重新生成，参考页面：<https://www.vgover.com/news/227900>。未做真实 Windows 主机、其他 Provider 或办公软件的新一轮验收。
