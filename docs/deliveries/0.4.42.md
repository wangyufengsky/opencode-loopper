# 0.4.42 本地交付：项目知识库

日期：2026-09-17。基线：0.4.41 / `14ff9cb`。本次仅本地交付。

## 实现范围

- 新增 `/knowledge` 与历史深层路由，采用已确认的 C 默认聊天布局；来源展开左侧、引用展开右侧，1600px 双面板、1100px 以下覆盖抽屉，支持 Esc 与焦点返回。
- 独立会话、多轮消息、项目历史分页、实时状态/正文、真实工具活动、累计模型用量、持久化发送身份、回执核对、停止及重启恢复。已有会话冻结项目、模型和授权，不创建 Designer、Task、Stage 或 Judge。
- 项目当前代码（含未提交改动）、项目文档、登记外部目录、受管上传及项目绑定数据库。支持 MD、DOCX、XLSX、PPTX、文本 PDF，中文关键词、路径检索、SHA 解析缓存、上传去重及中断恢复。
- `KNOWLEDGE_READ_ONLY`、知识所有者与受管代次绑定，只开放 4 个知识工具和 3 个既有数据库只读工具。保存不可变引用片段、版本和时间；拒绝跨会话引用及越界资料读取。
- V114 追加 5 张知识表及新工具策略，旧角色、旧策略和历史数据保持兼容。规范见 [项目知识库合同](../knowledge-contract.md)。

## 验证

| 层次 | 结果 |
| --- | --- |
| 聚焦 Java | 最后一次 `KnowledgeIntegrationTest,KnowledgeControllerTest,CodeStructureContractTest`：12 项，0 失败/错误 |
| 聚焦前端 | 类型检查通过；知识 store 8 项通过，覆盖丢失回包、草稿恢复、迟到响应和停止状态 |
| 浏览器交互夹具 | 5 项通过；1920、1440、1280、768px，左右面板、引用、长内容、错误、停止、历史深层路由与刷新 |
| 完整门禁 | `./scripts/verify.sh` BUILD SUCCESS，耗时 17:09；Java 2076 项，0 失败/错误、3 跳过；前端 74 个文件 / 461 项全通过；工具链与会计守卫通过 |
| 成品 JAR | 134 个静态文件，包含知识库前端 chunk、V114；SHA-256 已核验 |
| 隔离运行 | 正式启动器 + 成品 JAR + 独立数据，7 组真实 HTTP 检查通过：健康/版本、当前文件与 SHA、上传/去重/预览/移除、幂等发送/历史、请求回执、SSE、深层路由及无任务记录。测试实例 72193 已退出 |
| 实际服务页面 | 四种宽度均通过；默认面板收起、资料面板、焦点返回、无页面横向溢出；项目历史页可见；浏览器 JS 错误 0 |

三项跳过：Windows 专属启动器用例（本机 macOS）；需要 `loopper.cupxml2java.root` 的外部项目夹具；需要显式 `template.qualification` 的真实模板资格评测。没有把这些列为已验收。

首轮完整门禁在最终恢复复核发现问题后主动中止，保留日志；补齐回执恢复及迟到停止回包回归后重新执行上述完整成功门禁。隔离脚本的 CLI 参数和正常关闭等待窗口已修正，保留早期脚本日志；未因此修改成品代码。

## 产物与活动服务

- 正式 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/target/opencode-loopper-0.4.42.jar`（319902414 字节）。
- SHA-256：`f1d48e1cb08552b72378b3696a8c60876398e86103a5705ce6002e6011d24257`。
- 活动 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.42.jar`，与正式产物哈希一致。
- 地址：http://127.0.0.1:53156/knowledge；实际版本 0.4.42；JVM PID `72342`；受管 OpenCode PID `72403`。
- 健康 `UP`，OpenCode `AVAILABLE`，内部 MCP `CONNECTED`；原地址、数据目录、JDK 与设置保持。
- 旧 JVM `10034` 及其受管子进程 `10112` 均确认退出后才启动新进程。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.42-20260917-123054/data`。备份逐文件哈希一致，SQLite `quick_check=ok`；启动器一并备份于其父目录。
- 升级前后保持：2 个项目、3 个任务、76 条执行会话、47 条任务制品、2 个模板报告包；任务与会话状态一致。已保存设置与启动覆盖文件哈希一致。

原始门禁、隔离运行、进程替换证据、源码哈希和四尺寸截图保存在 `data/deliveries/0.4.42/`；`browser-live.json` 记录实际服务验收，`replacement-final.json` 记录完整替换证明。

## 验收边界

模型问答使用 deterministic fake 与离线夹具，未调用真实 Provider，也未消耗真实模型预算。实际受管 OpenCode 与私有 MCP 仅验证启动和连接状态，不能替代真实模型端到端问答证明。现有服务未登记数据库连接；数据库执行器的只读与权限边界由离线回归覆盖，真实数据库登录、SQL 执行和跨来源问答未联调。

当前首版不包含跨项目问答、Git 分支切换、OCR、向量检索、外网检索或文件生成。未推送、创建标签、发布 Release 或远端部署；未生成内置 JDK 平台包。
