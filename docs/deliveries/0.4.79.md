# 0.4.79 角色管理交互优化

## 交付内容

- 移除“查看技术标识”；保留职责描述和阶段所用版本的查看入口。
- 桌面目录与详情独立滚动，目录分页固定在底部；窄屏上下排列且目录有界滚动。
- MCP 与权限使用独立折叠面板，支持鼠标与键盘操作。
- 当前和历史 Prompt 合并为连续静态正文；片段键按自然数字顺序排列，不改动保存内容或运行时装配。
- 识别清单变量及正文显式占位符，将 `${name}`、双花括号占位符统一投影为 `{name}`，并附中文用途；没有显式变量时隐藏变量区域，JSON 示例保持原文。
- 用带连接箭头的流程图展示工作流位置，高亮当前角色负责的节点；相同工作流及位置合并展示，保留各阶段版本入口。

## 验证

- `./scripts/dev-check.sh frontend src/views/RoleManagementView.spec.ts`：8 项通过。
- `./scripts/dev-check.sh frontend src/utils/rolePrompt.spec.ts`：4 项通过，包括内置模板未返回变量元数据时的显式占位符识别。
- `npm --prefix frontend exec -- playwright test --config frontend/playwright.config.ts roles.spec.ts`：10 项通过。覆盖 3 套皮肤、1440/390 像素布局、独立滚动、分页可见、折叠及键盘展开、变量格式、导入错误恢复、版本冲突及历史查看。
- `./scripts/verify.sh`：BUILD SUCCESS，Java 2,374 项，0 失败、0 错误、3 条件跳过；前端 102 个文件、612 项通过。
- JAR ZIP 完整性、版本、首页及 149 个静态资源条目校验通过；正式构建期间候选源码哈希保持一致。
- 实际运行 API 核验全部 52 个角色阶段工具清单，以及项目、任务、会话、PPT、知识库和角色相关 12 张表的计数及状态分布未变。
- 实际浏览器验收：开发工程师流程位置、双栏独立滚动、33 项 MCP 与 49 条权限独立折叠、连续 Prompt 与 `{LOOPPER_PORT}` 中文说明。
- 最初的 0.4.78 候选已通过完整门禁；真实数据验收发现正文显式变量缺少接口元数据，因此补齐识别后递增到 0.4.79，并重新执行完整门禁。此前日志保留在 `data/role-ui-followup-20260924/`。

## 产物与本地运行

- JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/target/opencode-loopper-0.4.79.jar`
- SHA-256：`cf81b53f2875baeec593eca0820061846ff0f2ce4d5ac843f6604c26afe83a0c`
- 实际运行 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.79.jar`
- 版本：`0.4.79`；JVM PID：`4410`；managed OpenCode PID：`4488`。
- 地址：http://127.0.0.1:53156/roles
- 健康：UP；OpenCode：AVAILABLE；内部 MCP：CONNECTED。
- 旧 JVM 82090 及其 OpenCode 82168 已确认退出，端口释放后才启动新实例。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper-data-backups/53156-before-0.4.79-20260924-162855`。239 个文件 SHA-256 核验一致，SQLite integrity_check 为 ok。
- 沿用原数据目录、配置、端口和正式 macOS 启动器，由 launchd 管理并保持运行。
- 最终构建和运行证据：`data/role-ui-followup-079-20260924/`；浏览器最终回归日志：`data/role-ui-followup-20260924/browser-final.log`。

## 范围说明

本轮未调用真实 Provider，也未做 Windows 或 Linux 启动验收。3 条件跳过项为模板真实资格运行、Windows CMD 执行及外部 CupXml2Java Maven 证据场景。本地交付后，按用户追加的“推送发布”授权完成下述远端发布。


## 远端发布

- 发布标签：`v0.4.79`，固定指向已验证提交 `88a4d1fe910e56efb918fef85293159bc72f2736`。代码与标签通过原子推送发布，未强推或移动旧标签。
- [GitHub Release](https://github.com/wangyufengsky/opencode-loopper/releases/tag/v0.4.79)，正式版本，非草稿、非预发布。
- [Release Actions](https://github.com/wangyufengsky/opencode-loopper/actions/runs/35976157483)：Windows 启动脚本校验和 release 两个 Job 均成功。
- 远端完整构建：Java 2,374 项，0 失败、0 错误、3 条件跳过；前端 102 个文件、612 项通过，BUILD SUCCESS。
- 4 个资产全部下载回读，大小及 SHA-256 与 GitHub digest 一致；JAR 和两个启动脚本同时与 SHA256SUMS 一致。
- 远端 JAR：351,638,646 字节；SHA-256：`a98b3383d54545cae08c7b8a06d70af8aff22bcb4f3f0e8be7ff2f15ff5fb11f`。ZIP 完整性、版本、首页及静态资源校验通过。
- 两个启动脚本与标签源码一致，Windows 文件按 `.gitattributes` 使用 CRLF。远端 JAR 由 CI 独立重建，哈希与本地运行 JAR 分别记录。
- 证据及下载资产：`data/release-0.4.79-20260924/`。本次未进行远端环境部署。
