# 0.4.40：问题证据的 Git 作者追溯

## 范围

基于 0.4.38 增加完整报告和阶段报告的问题作者信息：冻结证据版本、文件/行范围、最后修改提交 SHA、Git 作者姓名/邮箱、作者时间（UTC）和提交简述。多人修改按连续行分别显示，支持子目录、改名以及基线引用。

作者信息来自任务自有冻结 Git 历史，不读取工作区 HEAD，不新增 AI Session，不改变模型候选、历史报告或任务状态。Git 最后修改者不自动认定为缺陷引入者；报告明确标注引入者未确定。无问题 coverage 不追溯，相同任务/版本/路径/blob/范围使用有界内存缓存，失败不缓存。读取在数据库事务外进行；历史或完整记录不可用时保留问题与原始证据并显示局限。没有新增任务 Token、费用或总时长硬预算，没有真实 Provider 调用。

## 验证

聚焦及报告集成回归 33 项通过，包含真实临时 Git 的多作者、改名、子目录、冻结版本、缓存命中、历史缺失、无效引用、超时降级、停止未确认传播、取消后报告与无新增 Session，以及事务暂停检查。首轮一项作者名断言未考虑 Git 去除尖括号的正常行为，已修正并通过回归，原失败日志保留。

成品初次验收发现旧证据把末尾 LF 后的空项计入行号。补充兼容：仅在不可变 blob 证明为这一种情况时，追溯全部实际代码行，并明确标注空项没有独立 Git 作者；其他不完整范围仍不可用。同时清空 Git ignore-revisions 配置影响，保持最后修改者语义。0.4.39 构建及初次隔离失败输出保留在 `data/acceptance-authors/`，该候选未替换本地服务。

证据目录：`data/acceptance-authors-0.4.40/`。所有正式验收均已完成。

## 完整门禁与成品

- `./scripts/verify.sh` 通过：Java 2060 项，0 失败、0 错误、3 条件跳过；前端 73 个文件、453 项通过；Node 工具 6 项、计费检查 7 项通过。真实模型资格试跑、Windows 平台用例、外部 cupxml2java 夹具按条件跳过，不作为已验收。
- 正式 JAR：`target/opencode-loopper-0.4.40.jar`；318219255 字节；130 个静态资源；ZIP 完整性检查通过。
- SHA-256：`bdc5407b91611999785c3d418f15a21d7512cea5f57197a75b9d3141c777059c`。
- 成品隔离验收使用旧库和冻结 Git 仓库的独立副本，fake Provider，关闭调度与启动恢复。56 项候选问题均有作者记录，没有整段追溯不可用；Session 未增加、任务状态与 Schema 113 保持。隔离 JVM 63046 已正常停止，退出证明在 `isolated-stop.json`。

## 实际服务与报告

- 已正常停止旧 JVM 16331 和其托管 OpenCode 16394，确认退出及端口释放后备份、启动。
- 地址：`http://127.0.0.1:53156`；实际版本 0.4.40；JVM PID 63372；托管 OpenCode PID 63435。健康 UP、运行时 AVAILABLE、内部 MCP CONNECTED。
- 活动 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.40.jar`；与正式产物 SHA-256 相同。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.40-20260916-181528`；启动器备份位于同名加 `-launchers` 的目录。文件清单一致，SQLite quick_check=ok。
- 保留 2 个项目、3 个任务、76 个会话、47 个任务制品及 2 个报告包；状态、设置、startup-overrides.properties 和 Schema 113 均保持一致。
- 原周审 `dba3e380-0110-4922-b74e-dc9519535d6f` 仍为 CANCELLED：404 个已分析片段、349 个未完成片段、6 个排除片段；56 项尚未独立复核的候选问题，共显示 448 条按连续行合并的作者记录，17 处末尾空项明确说明。候选仍不是已确认缺陷。
- 实际服务单次观测：首次报告约 3.97 秒，缓存后约 0.12 秒；新增模型 Session 为 0。这是本机这份报告的单次样本，不代表通用性能指标。
- 报告：`data/acceptance-authors-0.4.40/周审阶段报告-含作者.md`；正文 SHA-256：`71cc6e0ebfc4adbef706f9fc0e2d3ef052c13e1dc97f6ea1618db37e1729b39b`。浏览器下载为同目录 `周审阶段报告-在线下载.md`，截图 `active-partial-report.png`。浏览器显示与下载通过，无页面错误；每次生成时间不同，独立读取/下载的正文哈希可能不同。
- 构建、隔离、在线报告、浏览器证明分别为同目录 `build-proof.json`、`isolated-proof.json`、`active-report-proof.json`、`active-browser-proof.json`。服务替换证明为 `data/test-services/0.4.15-20260912-191210/replacement-0.4.40.json`。

未调用真实 Provider，未恢复原审查，未推送、创建标签、Release 或进行远端部署。服务保持运行。
