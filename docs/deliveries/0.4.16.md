# 0.4.16 — 文档模板按程序校验完成

日期：2026-09-12。

## 范围与行为

- 新建代码审查、项目人员贡献周报冻结模板 V7：分批分析、候选与报告校验、保存、执行收尾后自动完成。不再创建双 Judge 批次、评审会话或聚合评审输入；手动重试双评审接口拒绝此类任务。
- 历史 V1–V6 保留冻结的双评审要求和原恢复通道。普通任务不改变。没有迁移或手改既有任务、数据库、报告或活动 JVM。
- 完成流程复用停止证明、队列/租约释放及终态一致性守卫；生成完成不等于分析结论已获独立 AI 复核。MCP 修正、长度耗尽续接、限时、取消、覆盖/归属/评分与文件写入保护继续生效。
- 报告制品和阶段验证结果分别通知元数据刷新；overview 返回报告数量和冻结评审策略，数量变化可补偿丢失的制品事件。正文仍按需读取，已有报告、列表加载失败与待生成分别展示。
- 新模板详情移除双评审按钮、空评审卡和 0/2 指标，完成报告显示“已校验并保存”；旧报告保留评审语义。模板完成逻辑归 `TemplateTaskCompletionService`，`TaskService` 规模上限由 2671 降至 2668 行。

## 验证

- 受影响集成回归：模板创建 7 项、详情读取 18 项、候选/续接与停止边界 21 项通过。
- 最终聚焦：执行 14 项、停止证明 3 项、提示 2 项和结构门禁 4 项，共 23 项通过（`/tmp/loopper-template-final-focused-2.log`）。
- 前端类型检查通过；五个相关测试文件共 129 项，发现并修正 Vue 缺省布尔属性导致的旧报告标记问题后，受影响报告组件 7 项重跑通过，其余 122 项已通过。
- Chrome E2E 4 项通过：两种报告的主子链接、整包下载与 1440px/390px 布局，以及新模板无双评审展示。
- 大于 128 KiB 的 Git 证据、新旧完成策略、原会话修正、验证后恢复不追加模型调用、空范围零模型调用、报告元数据及验证刷新事件纳入回归。
- 完整门禁分段完成：`./scripts/verify.sh` 的后端 264 类、1720 项中 1717 项通过、3 项按条件跳过，失败和错误均为 0（`/tmp/loopper-0.4.16-verify-final.log`）。跳过项为真实 Provider 模板资格、Windows CMD 和未配置外部项目路径的路由资格测试。
- 全量前端首轮 363 项断言通过，但任务详情测试未卸载组件，环境销毁后遗留计时器触发 `document is not defined`。仅为测试增加自动卸载，未改变产品运行代码；类型检查和该文件 20 项重跑通过。随后用 Maven 固定工具链执行 `frontend:npm@frontend-vitest frontend:npm@accounting-guard-test`，52 个文件、363 项测试及 7 项统计守卫全部通过，无未处理错误（`/tmp/loopper-0.4.16-frontend-gate.log`）。
- `./mvnw -P'!backend-dev' -Dloopper.frontend.skip=false -DskipTests verify` 完成正式打包（`/tmp/loopper-0.4.16-verify-frontend-resume.log`）；该参数也跳过前端测试，故测试证据由上述独立 Maven 门禁提供。未重复执行源码未变且已通过的后端回归。JAR 版本为 0.4.16，包含静态入口、120 个 assets 条目及新增完成服务。
- 最终 JAR 隔离验收通过：fake Provider 的持久化 V7 贡献报告任务为 COMPLETED、无 Judge、4 份报告，API 数量一致；Chrome 1440px/390px 均显示“已校验并保存”，无双评审入口和横向页面溢出，选择报告前不读正文，选择后读取 1 次（`/tmp/loopper-0.4.16-runtime-acceptance.log`）。隔离 JVM 使用临时数据库和随机 loopback 端口，验收后仅清理本次进程；SIGTERM 等待 20 秒未退出后以 SIGKILL 停止并 wait 确认。该验收不覆盖真实 Provider 的分析质量。

失败输出保留在 `/tmp/loopper-template-backend.log`（沙箱 sysctl 限制）、`/tmp/loopper-template-focused.log`（结构上限）、`/tmp/loopper-template-final-focused.log`（完成服务装配）、`/tmp/loopper-template-frontend.log`（旧报告缺省属性）。完整门禁首次运行的环境失败另保留在 `/tmp/loopper-0.4.16-verify.log`：`JAVA_TOOL_OPTIONS` 在测试子进程中打印提示，破坏已有空输出断言；已停止该构建，改用 `MAVEN_ARGS` 重跑，未改动交付源码。上述问题已分别通过权限审查后的运行、职责提取、复用原停止证明协作者及明确默认值处理；最终重跑结果单独记录。

## 交付

- 已只读检查远端 `v0.4.16` 标签不存在、对应 GitHub Release 不存在；未创建标签或 Release。
- JAR：`target/opencode-loopper-0.4.16.jar`。
- 大小：291491595 字节。
- SHA-256：`5d8fbd6e433e7506ce3e008c8a7a86a015f8df252679d84773638c0a808f4ee1`。
- 未推送、未发布、未部署或替换既有服务，未对真实 Provider 做新调用。运行时验收使用隔离数据、随机 loopback 端口和 fake Provider。
