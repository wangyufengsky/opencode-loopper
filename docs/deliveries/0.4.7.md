# 0.4.7 内置模板任务

本次把模板和自动化入口改为项目内置的手动模板任务，提供代码审查与项目人员贡献周报。版本化预制合同接收项目、分支、日期和可选故事绑定参数，服务端冻结后正式开始，不再为重复执行重新生成设计。

项目、分支支持搜索和分页，默认主分支；日期采用北京时间，默认最近七天，结束日期不得早于开始日期。程序在任务独立目录同步所选分支、冻结提交与证据；模型分批分析，服务端校验覆盖和依据、计算评分排名、渲染 Markdown，经过同批双评审后自动完成。内容最多返修两轮，失败和历史版本保留。贡献任务生成总报告及个人报告；评分标准和每项依据随报告输出。

旧定时、Webhook 和分支变化触发停用，历史数据保留。V77 支持全新数据库和历史升级。源仓库的当前分支、索引、提交和未提交文件不会因模板执行改变。

相较用户已收到的 0.4.4，补齐报告摘要中的中文名称、返修版本和哈希；模板详情移除普通任务设计入口与人工认定面板。同时明确模型响应必须逐字复制程序给定的身份字段，并在返修提示中带上服务端的具体校验错误；评分、身份校验和两轮返修上限保持原样。

## 验证

最终 0.4.7 在独立构建目录执行 `./scripts/verify.sh` 通过，耗时 8 分 14 秒，日志 `/tmp/loopper-template-0.4.7-verify.log`。Java 1656 项，失败/错误为零，3 项条件跳过；前端 49 个测试文件、341 项通过；版本工具 6 项和故事命令守卫 7 项通过。最后修复报告卡片的网格最小宽度，让贡献排名宽表在卡片内滚动，正文按页面宽度换行；分析、评分和返修代码保持不变。

冻结的 1228 个源码/测试/构建文件与实际构建副本逐个 SHA-256 一致；JAR 已检查 SPA 静态资源、模板页面资源、模板协调器和 V77 迁移。新增报告摘要集成测试 17 项、详情页类型检查/回归 18 项、身份及返修反馈聚焦 10 项均通过。执行中间证据、失败记录和研究来源见 [执行记录](template-tasks-execution.md)。

使用最终 JAR、独立数据库/目录及随机端口启动，健康接口 UP，运行环境接口确认 Loopper 版本 0.4.7。实际 Chrome 验收旧入口跳转、项目与分支选择、两类空范围任务至 COMPLETED、中文报告名称与版本摘要、总报告跳转个人报告、Markdown 下载，并验证 1440/390 宽度下报告卡片不溢出、宽表独立横向滚动。页面脚本错误为零。日志 `/tmp/loopper-template-0.4.7-jar-browser.log`，截图及下载样本 `/tmp/loopper-template-0.4.7-jar-browser/`。

成品烟雾运行使用 fake Provider；非空报告预览使用 a6 真实模型已完成结果的 SQLite 备份，未伪造模型结论，也未在烟雾运行中重复调用真实模型。浏览器断言之外已人工检查实际报告截图。验收结束后停止本次独立 JVM，原有服务未动。

真实模型资格 a6 已通过：代码审查 5/5、贡献周报 5/5，全部第 0 轮完成；20 个实际分析会话 COMPLETED、20 个最终 Judge 均 PASS，质量断言及全部源仓库保全断言通过。正常样例不误报，植入缺陷样例只记录唯一实现错误，并且不将实现缺陷扣给正确测试作者。原始结果 `/tmp/loopper-template-qualification-20260911-a6/qualification.json`。0.4.7 与资格通过代码之间仅有报告卡片 CSS 及发布版本引用差异，未重复调用真实模型。

资格数据库的 20 个分析 Session 全部 COMPLETED、10 个任务租约全部 RELEASED。本次各轮资格 JVM 已退出，并按测试目录核对后停止五个遗留 OpenCode 子进程；记录 `/tmp/loopper-template-owned-process-cleanup.json`。数据库、失败日志和历史候选产物保留。

## 产物

- JAR：`target/opencode-loopper-0.4.7.jar`，291,389,158 字节。
- SHA-256：`1530371fe9ccaf0907e3de98ca08c94c162aa6e609381f2d7e3223779c85ab2e`。
- 校验文件：`target/opencode-loopper-0.4.7.jar.sha256`。
- 用户已收到的 0.4.4 JAR 和校验文件保留，SHA-256 未改变。

## 运行与已知边界

需要 Java 21、Git 2.40+、可用 OpenCode 及内网可访问的模型配置。JAR 同目录执行 `java -jar opencode-loopper-0.4.7.jar`，默认地址 `http://127.0.0.1:8080/`、数据目录 `./data`。启动脚本为 `scripts/start-windows.bat` 和 `scripts/start-linux.sh`；升级前备份数据，V77 停用原自动化触发。

统计范围是所选分支可达提交的 committer 时间，不代表首次合入该分支的时间。贡献评分是本项目定义的可观察代码贡献评价，不是行业统一绩效标准，也不推测工时、业务收益、未记录的评审或未提交人员表现。浅历史、octopus merge 和超限证据明确阻断；最终 Judge 提示上限 128 KiB UTF-8，大范围需要拆分执行。

本次仅本地交付；未推送、发布、部署或替换既有服务。没有内置 JDK 平台包，未在内网、Windows/Linux 或真实故事统计系统执行验收。
