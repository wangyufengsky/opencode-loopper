# 0.4.33：历史提交审查与冻结版本代码审查

## 交付范围

- 原 CODE_REVIEW 保留 ID，新合同更名为历史提交审查。Git 证据补齐作者、提交者、共同作者、原始及 mailmap 映射身份，报告展示完整提交追溯和人员索引；同内容问题合并保留全部关联。
- 新 SNAPSHOT_CODE_REVIEW 提供日期增量与全面审查。北京时间结束日期包含全天，以冻结第一父链版本的最终树比较，四阶段执行功能规划、分析、独立复核与报告。
- 任务内代码读取凭据、SHA/blob/行号校验、单元唯一归属、补充计划修订、关系检查、重复问题来源、失败批次选择重试及四阶段进度。
- V110 保持旧模板、报告、请求键、候选回执和列表视图，新增独立版本、文件、计划修订与读取记录。历史批次编排职责提取，公共调度器的构造依赖未增加。
- Git 路径按字面读取，特殊文件名不能扩展到其他文件或受保护内容。

规范：[冻结版本审查](../snapshot-code-review-contract.md)、[历史报告格式](../template-report-format.md)。

## 验证记录

- 聚焦后端：最终受影响集运行 73 项，0 失败、0 错误（包含历史执行/批次、版本审查、人员证据、升级与结构门禁）。其他阶段分别覆盖日期、分段、迁移、文档模板和 MCP。
- 前端：创建页日期/全面模式、进度面板、批次列表按需读取及旧请求响应隔离的聚焦类型和行为检查通过。
- 期间发现并修复：迁移视图列引用、旧迁移数夹具、新测试分页类型、旧新协议接合和字面路径保护。
- 完整门禁 `./scripts/verify.sh`：2026-09-15 15:51 完成，BUILD SUCCESS，用时 16:03。后端 316 个测试类、1,988 项（1,985 通过，3 跳过，0 失败、0 错误）；前端 69 个文件、437 项全部通过，类型检查与构建通过；发布工具 7 项通过。
- 3 项条件性跳过：真实模板 qualification 未启用、Windows CMD 在 macOS 不适用、外部 cupxml2java 样本路径未配置。
- 成品隔离验收：日期区间 2026-09-01 至 2026-09-10，期间引入后修复，两个不同 SHA 的最终树相同；任务 COMPLETED，4 阶段 SUCCEEDED，0 模型会话，4 份报告。正文显示结束日 24:00、冻结 SHA 和静态审查局限；ZIP 内容及摘要校验通过。
- 浏览器：5 个模板入口、新代码审查默认日期增量、全面模式隐藏日期、四阶段进度、报告预览与详情跳转通过；报告整套下载接口返回有效 ZIP。已有贡献周报仍显示 CONTRIBUTION_REPORT_V3，旧代码审查冻结标题保持。
- 隔离验收 PID 47228 已正常退出，隔离数据与报告保留。首个验收启动脚本的 fake 环境缺项和请求键格式错误已修正；不计入产品行为通过证据。此前中断或失败的候选构建日志均保留。

## 适用边界

首版仅静态代码审查，不执行目标项目构建、测试或脚本。Git 日期推算不能证明历史部署状态。未知语言和跨模块调用关系使用文本与结构提示，仍依赖模型分析和独立复核。模拟 Provider 与人工标注候选用于验证协议、输入与结果传递，未进行真实模型准确率验证。

本次只交付本地代码、完整 JAR 与本地运行服务；不包含远端推送、标签、Release、远端部署或内置 JDK 平台包。


## 成品与本地服务

- 正式 JAR：`target/opencode-loopper-0.4.33.jar`，318,088,930 字节；包含 `BOOT-INF/classes/static/index.html` 和 127 个静态资源。
- SHA-256：`af80cd65b213e3f332271ac55aa4139213a3232d9fd4b9e28098b4526fce452c`。
- 交付访问地址：<http://127.0.0.1:53156/template-tasks>。
- 活动 JVM：PID 47501；运行接口版本 0.4.33，健康 UP，OpenCode AVAILABLE（受管子进程 PID 47558），内部 MCP CONNECTED。
- 活动 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.33.jar`，与正式 JAR 哈希相同；由 `scripts/start-macos.command` 加载原启动配置启动。
- 旧 JVM 4193 与受管子进程 4249 正常退出后才启动新服务；优雅关闭等待 30 秒的 SSE 结束过程已保留日志。
- 完整数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.33-20260915-155655`。备份可读；原 1 个项目、2 个任务、24 条会话、45 个产物和 2 套报告保持，任务状态保持。
- 数据库从 V109 升级到 V110，`quick_check=ok`、无外键错误；app_settings 摘要和 startup-overrides.properties 哈希保持。
- 旧构建产物保留：`data/build-preservation/before-0.4.33/`。
- 可复查证据：`data/runtime-acceptance/snapshot-review-0.4.33/`（完整门禁日志、成品摘要、任务概览/审计、报告及 ZIP）；本地更新凭证 `data/test-services/0.4.15-20260912-191210/replacement-0.4.33.json`。
