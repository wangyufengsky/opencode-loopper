# 0.3.82 开发公约与验证流程优化

范围：精简根公约、目录规则、合同语义澄清、开发验证分层、发布版本同步与机械门禁。无业务 Java 实现、前端组件、数据库迁移、Provider 配置或产品授权语义变化。

- 根公约 149,931 → 10,636 UTF-8 字节；后端规则 5,678 字节，前端规则 7,711 字节。原根公约完整字节与基线提交 `bc2f761` 一致，归档见 [历史索引](../history/README.md)。
- 澄清自动双 Judge PASS / 独立人工认定、普通/滚动全自动、画像兼容修复范围、MCP/Schema 通道、独立预算轴、V2 默认与历史结果表级联。
- 增加 `backend-dev` 独立输出、`dev-check.sh`、版本同步/检查、文档大小/链接检查。完整 Maven 链路保留前后端测试、类型检查、静态资源与 Boot JAR；移除重复类型检查执行。
- 结构门禁增加指定直接字节码依赖检查，保持既有规模阈值。首轮检查将冻结 Row 值误归为数据库访问；已明确允许既有 `DesignWorkPackageRow` 输入，仍禁止数据库适配器。
- 当前开发流程与 Astra 比较评测计划见 [开发文档](../development.md)。尚未运行 Astra 旧/新流程 A/B，未声称 Token、耗时或生产 Provider 兼容性提升。

## 验证结果

- `node scripts/check-project.mjs`：通过，16 份路由文档的文件链接、三份公约体积与全部发布引用一致。
- `node --test scripts/tests/project-tooling.test.mjs`：4/4 通过；覆盖进位、非法版本、引用漂移/缺失、无写入计划、历史/依赖/CRLF 保持、本地 tag/JAR 占用及公约大小/坏链接。
- `./scripts/dev-check.sh backend CodeStructureContractTest,ReleasePackagingContractTest`：12/12 通过。日志证明跳过 Node/npm、前端构建/测试和静态资源复制；正式输出未被快速测试覆盖。
- `./scripts/dev-check.sh frontend src/views/TaskDetailView.spec.ts`：类型检查和 16/16 用例通过。
- `./scripts/verify.sh`：一次完整门禁成功，耗时 7 分 47 秒。Java 1,535 个用例，1,533 通过、0 失败、0 错误、2 条件跳过；前端 42 文件、276/276 通过；Node 项目工具 4/4、已有 accounting/semantic guard 7/7 通过。
- 跳过范围：Windows CMD 解析用例在 macOS 上禁用；cupxml2java 外部项目夹具因未配置 `loopper.cupxml2java.root` 而禁用。未将跳过计为通过。
- `bash -n scripts/dev-check.sh scripts/verify.sh`、`git diff --check`：通过。
- JAR：`target/opencode-loopper-0.3.82.jar`，289,494,723 字节。已核验 Boot JarLauncher、manifest 与 MCP YAML 版本、静态 index.html 和 112 个 assets 文件。
- SHA-256：`aa9dd32a13dce067d91041a6abc247c52e241eb02fb420e8475c21c35fdff7fe`。
- 原始验证日志保留在本地生成目录 `target/delivery-evidence/0.3.82/`，包括首轮规则误判与修正后的结果；该目录不提交。

已完成版本前置检查：远端 v0.3.82 无标签，GitHub 对应 Release 返回 404；本地同步脚本通过全部引用一致性、递增及占用检查。

运行时边界：本次没有重启/替换现有 JVM，没有浏览器端到端或真实 OpenCode/模型调用，没有推送、标签、Release 或部署。
