# 0.4.55 — 知识库侧栏返回原页面

## 交付范围

侧栏“知识库”由固定首页地址改为当前浏览器标签页最后访问的知识库地址。对话中切到项目、主页等模块，再点击知识库会回到原对话；在其他模块刷新也保留返回位置。历史页保留地址中的项目及搜索等筛选条件。

首次访问和主动“新建对话”仍进入知识库首页；直接打开对话链接及浏览器前进/后退按目标地址进入。仅记住页面地址，返回对话使用原有草稿恢复、服务端状态重读及 SSE 订阅逻辑，不自动发送、重发或停止模型。没有新增依赖、后端接口或迁移。

## 验证记录

- `./scripts/dev-check.sh frontend src/components/AppSidebar.spec.ts`：类型检查与 11 项测试通过，覆盖首次进入、最近对话、历史筛选、显式新建、刷新恢复、直接链接、无效地址及存储不可用时的内存回退。
- `npm --prefix frontend run test:e2e -- knowledge.spec.ts`：本机 Chrome 19 个知识库场景全部通过。其中新增 3 个导航场景覆盖离开期间模型完成、草稿恢复、重建订阅、没有额外发送或停止、新创建会话地址、其他页面刷新、新建首页和浏览器后退。
- `./scripts/verify.sh`：BUILD SUCCESS，耗时 17 分 15 秒。Java 2119 项，0 失败、0 错误、3 条件跳过；Vitest 78 个文件、507 项全部通过。跳过项为未开启的五轮模板资格测试、macOS 不适用的 Windows CMD 测试、未配置外部 CupXml2Java 工程根目录的路由测试。
- 正式 JAR 的静态资源检查通过，确认打包入口资源包含本次导航记忆逻辑；打包前后的 1710 个候选源码及构建文件哈希一致。
- 更新后的实际服务使用本机 Chrome 验收通过：已有对话切到项目/主页后返回同一对话，未发送草稿保留；在主页刷新后仍返回原对话；历史页项目筛选保留；主动新建进入首页；独立浏览器上下文初次访问进入首页。4 次会话订阅、0 页面异常、0 写请求；页面截图已检查。

浏览器中的模型状态变化使用 HTTP/SSE 夹具验证；本次不调用真实 Provider。原始输出保存在 `data/deliveries/0.4.55/`。

## 本地交付

远端 `v0.4.55` 标签查询成功且为空，GitHub Release 查询为 404；只用于版本查重。

- 成品：`/Users/wangyufeng/IdeaProjects/opencode-loopper/target/opencode-loopper-0.4.55.jar`，320020731 字节，包含首页及 136 个静态资源。
- SHA-256：`127e2c296a0149b5f9b72566eb0e73837fbe974e6e0636b4f853f7c9d595f4ab`。
- 活动 JAR：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.55.jar`，哈希与成品一致；实际版本 `0.4.55`，JVM PID `11354`，受管 OpenCode PID `11414`。
- 地址：`http://127.0.0.1:53156/`；健康 UP、OpenCode AVAILABLE、内部 MCP CONNECTED。交付服务保持运行。
- 旧 JVM 88962 和它拥有的受管子进程 89022 均确认退出；完整备份原数据后由正式 macOS 启动器启动新版本。
- 数据备份：`/Users/wangyufeng/IdeaProjects/opencode-loopper/data/test-services/0.4.15-20260912-191210/backups/before-0.4.55-20260918-011309/data`；完整文件清单和 SQLite quick_check 核对通过。
- 更新前后的项目 3、任务 7、知识会话 8、回合 12、引用 66 及其他历史计数一致，知识历史内容哈希、启动覆盖配置与系统设置保持。

未推送、未创建标签或 Release，未做远端部署或内置 JDK 平台包。
