# 0.4.27 原文直读与模板批次并发

## 交付范围

- 文档模板版本 2：上传后程序冻结文档及按标题／页码组织的分段，模型通过受限 MCP Resource 或同源 Tool 按需读取。需求开发直接进入既有 Designer；静态评审直接读取原文和冻结代码树，独立复核再检查原文遗漏与代码结论。移除新版流程的前置需求提取与清单复核，历史版本 1 保持原合同。
- 原文版本独立于评审结果矩阵；开发使用 DOC 来源引用与正式验收场景，静态评审结果才产生逐项需求结论。最终双 Judge 必须各自读取全部原文，读取凭据仅证明已读取，不代替语义正确。未提取图片先保留局限，影响实现或验收时再询问。
- 模板版本 8 的代码评审／贡献报告，以及文档版本 2 的需求代码评审，采用任务内并发窗口，默认 4，设置页可设 1–16。新任务冻结设置；历史／活动合同不被设置修改。需求开发固定串行，窗口不扩大权限、预算或停止边界。
- 创建、未知投递、运行、停止中的会话持续占用窗口；乱序完成独立补位，后继阶段等待全部依赖结果，报告按原始顺序汇总。恢复保留已验证批次，语义复核修正仍复用既有有界轮次；本版不承诺仅重做有语义问题的批次。
- 新增 V107/V108：不可变原文来源版本、当前角色读取凭据、开发来源外键，以及两个静态评审候选种类。没有修改历史迁移。

## 聚焦与隔离行为证据

- 80 个真实持久化分析批次的可控 Provider 集成测试：并发峰值 4，最高序号先完成时继续补位，任务内设置冻结，独立复核阶段等待分析完成。另有单批静态缺陷报告测试，证据不足不显示全部满足。`/private/tmp/direct-round8.log`、`/private/tmp/document-template-focused-final.log`。
- 第一期两个模板版本 8 集成测试均验证 6 批次、最多 4 个独立 MCP 会话、乱序完成及贡献分析依赖屏障。与 Git／非 Git 原文开发合计 19 项通过。`/private/tmp/direct-development4.log`。
- 新版需求开发的 Git／非 Git 单包路径：当前项目目录执行、原生 Maven 行为验证器、同批双 Judge、缺少原文阅读凭据的 PASS 被拒绝、独立结果处置与报告。Git 场景验证实施期间重启恢复。新增补充文档测试证明新原文版本直接重新设计、旧来源保留、无模型提取。`/private/tmp/direct-last-behaviors.log` 中这 3 项通过。
- 受影响后端 218 项首轮有 4 个兼容断言／角色工具隔离失败，已修正；后续 15 项聚焦通过，覆盖新旧角色专属提交权限、带历史数据的 V106 升级至 V108、直接评审。原文 Resource HTTP 和 Tool 共用读取／撤销测试首轮 MockMvc 默认编码断言失败，显式按 UTF-8 读取后 11 项通过。`/private/tmp/document-template-affected.log`、`/private/tmp/document-template-focused-final.log`、`/private/tmp/direct-resource-http.log`。
- 前端类型检查、原文面板 1 项、设置 4 项、文档详情 3 项通过；1440px／390px Chrome 上传重试幂等、刷新、按需原文及报告、下载和无横向溢出两项通过。浏览器首轮夹具路径误写为 files，改为真实 documents 路径后通过。日志 `/private/tmp/direct-browser-r2.log`，截图 `/private/tmp/loopper-document-template-1440.png`、`/private/tmp/loopper-document-template-390.png`。

## 真实模型效果

使用原模拟 DOCX 与三个合成 Java 文件。评测器复用生产解析器、提示、Schema、原文和代码引用校验器；隔离适配器提供冻结读取凭据。此证据属于 Luna 分析效果，不替代生产 HTTP／数据库编排验收，也没有执行样例项目测试。

首轮 `data/qualification/azx0-direct-luna-20260914/` 分析耗时 134.4 秒，模型已读 15 段并识别空字符串缺陷，但适配器提交工具缺少只读标注，被 approval_policy=never 拒绝；没有 ACCEPTED。修正仅涉及隔离评测适配器，保留首轮失败证据，进行一次有界复测：每角色 600 秒、4 次候选提交、200 次工具调用，无模型／账号／付费回退。

复测 `data/qualification/azx0-direct-luna-20260914-r2/`：分析 299.881 秒，读取全部 15 段及三个代码文件；4 次候选均被拒绝（第 1/2/4 次 SHA 抄写不符，第 3 次未说明全部原文章节），后续提交被预算拒绝。没有 ACCEPTED，独立复核未启动，不宣称 Luna 真实链路验收成功。模型输出中的空字符串缺陷与序列化证据缺口只能作为未接受的观察。隔离工作区未变，临时认证副本已移除，实际模型请求数不可得。摘要 `summary.json` 保留全部拒绝原因；停止追加模型调用。

## 完整门禁与交付

首轮完整门禁执行 13 分 50 秒，后端 1,948 项，14 失败、2 错误、3 条件跳过。失败包括 12 项旧迁移版本／执行次数断言、2 项精简 Judge 配置容器缺少新增依赖；两个错误来自候选 Schema 测试表未登记两个新角色。已集中修正测试夹具，保留首轮日志 `/private/tmp/loopper-0.4.27-verify.log`。业务实现未因这些断言调整。

69 项修正聚焦测试全部通过，日志 `/private/tmp/document-full-gate-fixes.log`。第二轮 `./scripts/verify.sh` 通过，耗时 14 分 26 秒：后端 1,948 项、0 失败、0 错误、3 条件跳过；前端 66 文件／424 项通过，类型检查、静态构建及工具门禁通过。日志 `/private/tmp/loopper-0.4.27-verify-r2.log`；压缩留存于 `data/acceptance/document-direct-0.4.27/gates/`。三个跳过为当前平台不适用的 Windows CMD、未配置的外部工具仓库及独立真实模板资格测试。

- 正式 JAR：`target/opencode-loopper-0.4.27.jar`，302143947 字节，124 个静态文件，ZIP 完整且包含 V107/V108 与原文 Resource 实现。SHA-256：`df46ea79a7b742cddad8a430c43f8863b0382d06327ba601039386a5b8dfbe42`。
- 旧 0.4.26 构建产物在 clean 前备份至 `data/local-artifact-backups/before-0.4.27/`，SHA 与原交付记录一致。
- 成品 JAR 独立端口 61786、独立数据与合成 Git 项目：两种文档上传各保留 15 段、幂等重试同一记录、原文版本 1／需求结果版本 0、评审并发 4／开发并发 1；重启后保持，评审冻结 SHA 正确，源项目字节与 Git HEAD 未变。仅 fake Provider，未完成模型评审，真实调用 0。结果 `data/acceptance/document-direct-0.4.27/result.json`，最终隔离 PID 48052 已退出。
- 正式 macOS 启动器完成 0.4.26 → 0.4.27 更新：旧 JVM 98138 和受管 OpenCode 98205 均已退出，新 JVM 48126 在 `127.0.0.1:53156` 监听，受管 OpenCode 48179。健康 UP、运行时 AVAILABLE、内部 MCP CONNECTED。
- 运行 JAR：`data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.27.jar`，SHA 与成品一致；源目录与正式启动配置保持。
- 完整数据备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.27-20260914-193502`；启动器备份同名加 `-launchers`。备份 quick_check=ok；迁移后 schema 108，项目、任务、会话、制品和报告数量及任务状态保持，startup-overrides SHA 未变，原超时关闭设置保持。
- 活动 JAR Chrome 只读验收通过：设置默认并发 4、开发上传入口、评审分支入口且无需日期、历史任务页，页面异常为空。未创建用户项目任务或调用真实 Provider。日志 `/private/tmp/loopper-027-live-browser.log`。
- 本地替换收据：`data/test-services/0.4.15-20260912-191210/replacement-0.4.27.json`。

本次已获推送和发布授权；目标标签为 `v0.4.27`，远端重建 JAR 必须按其自身 `SHA256SUMS` 验证，不能用本地 JAR 哈希替代。远端运行和资产证据保存在 `data/release-verification/0.4.27/`，最终发布状态以对应 GitHub Actions 与 Release 为准；本地验收不等于内网部署或 Luna 资格通过。

首轮推送 CI `34839185144` 的 macOS 完整后端执行了 1,948 项，4 项开发编排测试失败：合成 Maven 项目使用机器默认的 resources 插件 3.4.0，离线缓存仅包含主构建使用的版本。已在测试夹具中固定 resources 3.5.0、compiler 3.15.0，与外层构建一致；保留离线限制与真实 Maven 行为验证。修正后该类 19 项聚焦通过，日志 `/private/tmp/loopper-027-ci-fixture-fix.log`。此次仅修改测试夹具和交付说明，生产代码及本地 JAR 未变；发布仍等待修正提交的三平台完整 CI。

Windows 首轮完整执行 1,948 项，8 失败、2 错误、19 条件跳过。其中包含同样的两份 Maven 测试夹具、Word 受管回执分隔符不兼容，以及 JDBC 驱动／同 JVM HTTP 重启夹具的文件句柄清理。后续确认 Word 属于生产代码问题，因此本候选未创建正式标签或 Release；修正与最终发布转入 [0.4.28](0.4.28.md)。保留本版本已通过的本地验收和 JAR，不将后续修正回填为本版本成功。
