# 0.4.57 知识 MCP 软优先级

按用户要求，将知识问答的工具选择调整为：工具同样适用时，优先考虑已授权的知识库、文档、Git、数据库 MCP，以便取得结构化结果和可追溯引用。

该偏好写入研究提示与 MCP 传输提示，不增加工具顺序、调用配额、权限校验或完成门槛。已知文件位置、原生工具更直接，或 MCP 不适用、不可用、结果不足时，AI 可直接原生调查；用户明确指定调查方式时遵从用户要求，不为优先级重复查询。保持 0.4.56 的自动自查、未完成待办续查及原生工具能力。

## 验证

- 复用相关集成回归：KnowledgeIntegrationTest、KnowledgeMcpTransportIntegrationTest、AssistRuntimeSupportTest 共 33 项通过，包含 MCP 离线原生调查、同会话自查、未完成待办续查、未知派发恢复及停止。
- 提示变更不新增镜像文案的单元测试；通过差异审阅、现有集成回归及正式 JAR 运行验收核对。
- `./scripts/verify.sh` 完整门禁通过：Java 2127 项（失败 0、错误 0、跳过 3），Vitest 507 项通过；包含类型检查、Node 工具回归、前端构建与可执行 JAR。耗时 17 分 28 秒。
- 已核验 JAR 首页与 136 个静态资源，以及研究提示和传输提示中的软优先级内容确实打入产物。
- 正式 JAR 使用独立数据、端口和真实模型 `opencode-go/deepseek-v4.1-flash` 完成两种提问验收。普通提问自主组合 MCP 11 次、原生工具 13 次；其中 1 次 Git 来源不可用及 1 次原生读取失败后继续调查，最终完成，未把单次工具失败当成停止条件。明确要求仅原生调查时，14 次调用全部为 `read`、`glob`、`grep`，MCP 为 0。
- 两种提问都完成同会话自查，正确识别测试项目的普通限额 5000、高级限额 7500、冻结账户禁止付款、README 两处矛盾及缺失定义。已核对实际派发的持久化请求含软优先级提示。该验收证明两条路径可用，不代表模型每次都会首先调用 MCP。
- 隔离浏览器验收通过：最终答案、中文原生工具记录、默认折叠、展开及刷新保留均正常，无页面脚本错误或业务写入；测试项目文件保持不变。隔离 JVM 65056 及其受管 OpenCode 65078 已退出。
- 本地服务已从 0.4.56 替换为 0.4.57：旧 JVM 43477 与受管 OpenCode 43550 已退出，新 JVM 65641、受管 OpenCode 65718；健康为 UP，运行时 AVAILABLE，内部 MCP CONNECTED。
- 更新前后 3 个项目、7 个任务、9 个知识会话、14 个回合、91 条引用、207 条工具记录及其他业务表计数一致；知识历史内容哈希、任务状态、设置与启动覆盖配置均保持，备份及活动数据库检查为 ok。
- 正式服务浏览器验收通过：历史内容可见，侧栏恢复、草稿、刷新返回、历史筛选、新建对话及独立标签页行为正常，无页面脚本错误或业务写入。

## 交付

本次仅交付本地 JAR、服务更新与本地提交，没有远端推送、标签、Release 或远端部署。

- JAR：`target/opencode-loopper-0.4.57.jar`（320042938 字节）。
- SHA-256：`8dfa06ef49a66210d778a785e36520ed5d18426fc340426dd13ce4c26654ed7d`。
- 服务地址：`http://127.0.0.1:53156/knowledge`；版本 0.4.57，JVM PID 65641。
- 活动 JAR：`data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.57.jar`，与交付 JAR 哈希相同。
- 完整数据备份：`data/test-services/0.4.15-20260912-191210/backups/before-0.4.57-20260918-111949/data`；逐文件哈希及符号链接清单核验一致。
- 详细证据：`data/deliveries/0.4.57/`，包含完整门禁、两种真实提问、隔离浏览器、服务替换及正式页面验收记录。
