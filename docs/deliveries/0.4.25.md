# 0.4.25 DOCX 合并表格与 Luna 真实需求提取测试

## 交付范围

- `ASSIST_DOCUMENT_V2` 为 DOCX 合并表格保存逻辑行列、跨行与跨列关系；普通矩形表格仍使用 Markdown。旧冻结表示不改写。缺失网格位置显式标注，异常合并、旧式 hMerge、嵌套表格与超限返回具体错误，避免静默丢失内容。
- 从截图制作的 3 页模拟 DOCX 保持原文件不变，SHA-256 为 `ce93645ffa22d10f9e4d277a940ee30ab5590412ebc1ed38a81fc51ac8e3b54b`。回归直接验证它的 UP/AC/O 列关系、15 个分段及需求和用例文字。
- 根据 Luna 实测失败，角色提示明确逐字引用、JSON 解码与 sources/coverage 双向一致；拒绝反馈定位需求编号与 fileId:section，不回显原文、不放松验收规则。
- 新增可重复的 Luna 效果探针，复用生产解析器、角色提示、候选 Schema 和需求校验器。模型通过受控 MCP 读取冻结分段并提交候选。

## 真实 Luna 结果

模型为 `gpt-5.6-luna`，reasoning effort 为 `medium`，Codex CLI `0.154.0-alpha.6.2`，使用已有 ChatGPT 订阅。每会话 600 秒、4 次候选提交，工具调用有 200 次准入上限；不使用 API Key、额度购买或模型回退。提取与复核为独立 Session，各读取全部 15 个分段。临时订阅凭据副本已删除，测试工作目录未被模型改写。

| 轮次 | 结果 | 耗时 |
| --- | --- | --- |
| 桥接器诊断 | 漏只读工具声明，CLI 拒绝 MCP；未生成候选 | 26.491 秒 |
| 表格优化后、反馈改进前 | 四次候选均被生产校验拒绝，未启动复核 | 309.912 秒 |
| 反馈与提示改进后提取 | 第三次候选接受，得到 10 项需求；前两次为逐字引用和覆盖关联错误 | 267.427 秒 |
| 独立原文复核 | 第二次候选接受，approved=true、corrections=[]；首投为分段 14 的归属格式错误 | 81.024 秒 |

最终提取与复核合计 348.451 秒。不是首投通过，也不是无修正自动成功。前述失败均保留，未拿最终通过覆盖历史失败。

人工按不变的 DOCX 中 R1–R4、T1–T8，加表头关系与范围共 14 项逐项核对，均在最终候选的正文、验收描述及逐字引用中保留。映射为 R1→RQ-1/RQ-4，R2→RQ-5，R3→RQ-6，R4→RQ-7；T1→RQ-4，T2→RQ-5/RQ-8，T3/T4→RQ-5，T5/T6→RQ-6，T7/T8→RQ-7。RQ-3 明确 O 在 UP 列，AC 空白按模拟约定表示不适用。目录保持背景，没有变成业务需求。RQ-1/RQ-4 有内容重叠，未证明需求条数最优。

语义核对由实施 Agent 完成，不是盲法外部评估。一个样本不能推广为普遍通过率。引用/覆盖的结构接受、独立模型批准、人工样本核对是三个不同证据层次；没有执行文中的业务测试。

Token 为 CLI 回合汇总：最后一对会话 input 294231（其中 cached input 219904）、output 16480；包含诊断和失败的四个会话累计 input 528112（其中 cached input 406016）、output 32719。cached input 是 input 的子集，不能再次相加。实际模型请求次数不可得，未以 Session 或工具次数替代。

本探针将 15 个分段合并为一个冻结作用域，未验证产品的分批编排、真实 OpenCode Provider、页面上传到正式 Task 的完整生命周期、代码开发或静态代码评审。它是 Luna 的真实需求提取/原文复核效果证据。

证据位于 `data/qualification/azx0-luna-20260914*`、`data/qualification/azx0-luna-summary.json` 和最终目录中的 `semantic-review.json`。该目录包含 Prompt、配置、模型事件、候选、工具回执与 Token，未提交 Git。

## 工程验证与运行交付

- 第一轮聚焦回归 27 项通过；诊断与提示改进后聚焦回归 31 项通过，包含 DOCX、来源校验、人工金标准修正、模型执行和结构门禁。
- Python 资格工具的闭合 Schema 边界测试 2 项通过；Java 探针独立编译通过。
- 首次完整门禁遇到沙箱禁止 loopback bind / sysctl 的环境错误，已停止精确 Maven/测试 JVM。失败日志 `/private/tmp/azx0-0.4.25-verify.log` 与报告 `/private/tmp/azx0-0.4.25-sandbox-failed-reports/` 保留。
- 允许本地测试端口后完整门禁通过，耗时 13 分 11 秒：后端 1919 项、0 失败、0 错误、3 条件跳过；前端 65 文件、421 项通过，类型检查、Vite 构建与工具回归通过。命令为带 Mockito agent 的 `./scripts/verify.sh`，日志 `/private/tmp/azx0-0.4.25-verify-r2.log`。
- 3 项跳过分别是 Windows CMD、未配置外部 cupxml2java 测试仓库及第一期模板五轮真实资格测试；本次 Luna 效果测试为单独入口，不能冒充该跳过项。
- 正式 JAR：`target/opencode-loopper-0.4.25.jar`，300944305 字节，124 个内置静态资源，ZIP 完整性通过。SHA-256：`4c07d07c841ed475d1ca5740faff0cbaaa35446215edaa49948f5bea0444d5a6`。
- Luna 最终测试使用的解析器、表格表示、角色提示、引用验证四个生产 class 与正式 JAR 内对应 class 字节完全一致，核验收据见最终资格目录的 `jar-verification.json`。

旧 0.4.24 JAR 已保存在 `data/local-artifact-backups/0.4.24-before-azx0/`，SHA-256 为 `d7e93b6adb03cfcb9362a8bf733bb069734df661e7335976e9e23f336e9ca9da`。无远端推送、标签或 Release 操作。

## 本地服务更新证据

- 正式 macOS 启动器已将 0.4.24 更新为 0.4.25，保留访问地址 `http://127.0.0.1:53156`；新 JVM 为 77561，受管 OpenCode 为 77632，健康 UP、运行时 AVAILABLE、内部 MCP CONNECTED。
- 旧 JVM 56825 与旧受管 OpenCode 56885 均已正常退出，停止证明成立后才启动新版本；未取消用户任务。
- 运行 JAR 为 `data/test-services/0.4.15-20260912-191210/opencode-loopper-0.4.25.jar`，哈希与成品一致。
- 完整数据和启动器备份为 `data/test-services/0.4.15-20260912-191210/backups/before-0.4.25-20260914-165249/`，备份数据库 quick_check 为 ok。
- 更新前后保持 1 个项目、2 个任务、24 个执行会话、45 个制品、2 份报告包、2 条目录租约；任务状态、schema 106 和 startup-overrides.properties 哈希保持不变。
- 真实浏览器只读验收需求开发、需求代码评审、历史任务页面，pageErrors=[]。两份历史报告下载 ZIP 完整，分别 29 和 9 个条目；本次页面验收未创建模板发起或模型调用。
- 运行收据：`data/test-services/0.4.15-20260912-191210/replacement-0.4.25.json`；启动日志为同目录 `startup-0.4.25.log`。
