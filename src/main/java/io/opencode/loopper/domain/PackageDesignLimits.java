package io.opencode.loopper.domain;

/** Shared published bounds for structured candidates, Markdown facts and tool schemas. */
public final class PackageDesignLimits {
    public static final int MAX_SCENARIOS = 64;
    public static final int MAX_FACTS = 128;
    public static final int MAX_STAGES = 6;

    private PackageDesignLimits() { }
}
