# OpenCode Loopper

OpenCode Loopper 是本机 AI 编程控制平面；本词汇表定义设计、执行与人工授权之间容易混淆的领域概念。

## 设计上下文

**权威需求**：
由用户正文、已记录的问答决定和最终确认稿共同形成的任务语义；只有它能够确定实施范围、授权和验收义务。
_避免使用_：把附件内容、模型推断或仓库观察直接称为需求

**设计附件**：
用户随一条设计消息提交的不可变文件快照，是不可信的参考上下文，不会仅因上传而成为权威需求。
_避免使用_：上传需求、项目文件、临时文件

**暂存附件**：
已进入当前输入区、等待用户随文字显式发送的文件；发送前可以移除，且暂存本身不会启动模型回合。
_避免使用_：已发送附件、拖放即发送

**附件消息**：
由必填文字和一个或多个已暂存设计附件组成、经用户显式发送的单次设计输入；本地解析与持久化按整批成功或失败，远端 OpenCode 交付失败另记为可审计的 Session 失败并由页面保留输入供重试。
_避免使用_：纯附件消息、独立上传事件

**附件作用域**：
设计附件与整体需求或某一个工作包之间的明确归属；整体需求附件是全局上下文，工作包附件只属于该工作包。
_避免使用_：默认全局附件、无作用域附件

**附件清单**：
由服务端投影的附件名称、类型、大小、作用域、内容身份和状态集合，用于在设计历史与 Task 中追踪实际采用的上下文。
_避免使用_：浏览器附件列表、文件路径列表

**显式附件授权**：
用户发送附件时授予 OpenCode 使用该文件快照的权限，即使文件名通常用于敏感资料；该授权不允许模型自行读取项目目录中的其他敏感文件。
_避免使用_：敏感目录读取授权、项目文件访问授权

**冻结附件上下文**：
设计确认时选定并保持原始字节与内容身份不变的设计附件集合，可供对应的开发、恢复和评审流程继续引用。
_避免使用_：重新读取原文件、可变设计附件

**附件停用**：
Task 确认前通过新讨论修订阻止一个已发送设计附件继续进入后续上下文；它不会抹除既有消息、已发生的远端投递或历史内容身份。
_避免使用_：撤回、删除历史附件

**附件替换**：
在相同作用域内以文件名完全相同的新不可变设计附件接替旧附件进入后续上下文，并自动停用旧附件；不同作用域互不替换，既有消息、旧文件内容身份和已经冻结的 Task 保持不变。
_避免使用_：物理覆盖、改写历史附件
